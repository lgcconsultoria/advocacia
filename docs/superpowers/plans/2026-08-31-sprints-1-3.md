# Sprints 1 e 3 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir os defeitos que quebram o site (conteúdo invisível sem JS, hero de 2,5 MB, zero headers de segurança, zero medição) e elevar o acabamento (dark mode, alinhamento de cards, colunas vazias, plataforma) sem regredir uma linha de copy.

**Architecture:** Duas decisões sustentam tudo. **(1)** Os tokens de cor se separam em rampas fixas de marca e uma camada semântica de superfície — só a segunda muda com o tema, o que torna o dark mode uma troca de ~9 variáveis em vez de uma auditoria de 579 linhas. **(2)** O site passa a ter **zero scripts inline**: um `public/bootstrap.js` de mesma origem cuida de tema e reveal antes do primeiro paint, e os inits de analytics viram arquivos em `public/vendor/`. Isso permite `script-src 'self'` sem nonce, o que mantém todas as páginas estáticas e o TTFB de 39 ms intacto.

**Tech Stack:** Next.js 16 (App Router), React 19, Tailwind CSS v4, Keystatic + Markdoc, Motion (só onde permanece), Lucide, Vitest, Playwright, `@axe-core/playwright`, ffmpeg 9.

**Spec:** `docs/superpowers/specs/2026-08-31-sprints-1-3-design.md`

## Global Constraints

Estas regras valem para **todas** as tarefas. Os requisitos de cada tarefa as incluem implicitamente.

- **Não regredir conteúdo.** Nenhum texto institucional, aviso legal, `metaTitle`, `metaDescription` ou arquivo `.mdoc` pode ser alterado. Os avisos do Provimento CFOAB 205/2021 no rodapé permanecem literalmente intactos. A única exceção são os campos novos e opcionais da Tarefa 15.
- **Não inventar fatos sobre o escritório.** Números de casos, liminares, anos de atuação, depoimentos e casos anonimizados não podem ser criados. Se um componente precisar deles, ele é entregue vazio e alimentado por CMS.
- **Estado padrão é visível.** Nenhuma regra de CSS pode esconder conteúdo sem que `<html>` tenha a classe `js`.
- **Zero scripts inline.** Nenhum `<script>` com corpo, nenhum `dangerouslySetInnerHTML` em script. Exceção única e deliberada: JSON-LD (`type="application/ld+json"`), que não é executável e não é governado por `script-src`.
- **Movimento:** deslocamento máximo 12px, duração máxima 400ms, stagger 40ms, máximo 6 itens por lote. Nada acima da dobra é animado. `prefers-reduced-motion: reduce` desliga tudo.
- **Acessibilidade:** WCAG 2.2 AA; AAA (≥7:1) no corpo de texto. Alvos de toque ≥44×44px. Nenhuma informação transmitida apenas por cor.
- **Orçamento:** home ≤900 KB no primeiro carregamento; nenhum asset isolado >600 KB; JS inicial ≤150 KB gzip.
- **Idioma:** todo texto visível ao usuário em pt-BR. Comentários de código em pt-BR, seguindo o padrão do repositório.
- **Commits:** mensagem em pt-BR no padrão Conventional Commits já usado no repositório (`feat:`, `fix:`, `chore:`, `docs:`).

---

### Task 1: Toolchain de verificação

Sem isso nenhuma tarefa seguinte tem como provar que funcionou. Os critérios de aceite do briefing (§11) são binários e precisam ser executáveis.

**Files:**
- Create: `vitest.config.ts`
- Create: `playwright.config.ts`
- Create: `tests/unit/smoke.test.ts`
- Create: `tests/e2e/smoke.spec.ts`
- Modify: `package.json` (scripts + devDependencies)
- Modify: `.gitignore`

**Interfaces:**
- Consumes: nada
- Produces: `npm run test` (Vitest, ambiente `jsdom`, raiz `tests/unit`), `npm run e2e` (Playwright contra build de produção em `http://localhost:3100`)

- [ ] **Step 1: Instalar as dependências**

```bash
npm i -D vitest@^3 jsdom @playwright/test @axe-core/playwright
npx playwright install chromium
```

- [ ] **Step 2: Criar `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
import { fileURLToPath } from 'node:url';

export default defineConfig({
  resolve: {
    alias: { '@': fileURLToPath(new URL('.', import.meta.url)) },
  },
  test: {
    environment: 'jsdom',
    include: ['tests/unit/**/*.test.ts'],
    globals: false,
  },
});
```

- [ ] **Step 3: Criar `playwright.config.ts`**

Os testes rodam contra o **build de produção**, não contra `next dev` — headers, cache e orçamento de bytes só existem em produção.

```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:3100',
    trace: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run build && npm run start -- --port 3100',
    url: 'http://localhost:3100',
    reuseExistingServer: !process.env.CI,
    timeout: 300_000,
  },
});
```

- [ ] **Step 4: Escrever os testes de fumaça**

`tests/unit/smoke.test.ts`:

```ts
import { describe, it, expect } from 'vitest';

describe('toolchain', () => {
  it('roda testes unitários em jsdom', () => {
    expect(typeof window).toBe('object');
    expect(typeof localStorage).toBe('object');
  });
});
```

`tests/e2e/smoke.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('a home responde e tem o H1 de posicionamento', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('h1')).toContainText('conflito e um prazo');
});
```

- [ ] **Step 5: Adicionar os scripts ao `package.json`**

Dentro de `"scripts"`, junto dos existentes:

```json
"test": "vitest run",
"test:watch": "vitest",
"e2e": "playwright test"
```

- [ ] **Step 6: Ignorar artefatos de teste**

Acrescentar ao final de `.gitignore`:

```
# testes
/test-results
/playwright-report
/blob-report
/playwright/.cache
```

- [ ] **Step 7: Rodar e verificar que passam**

Run: `npm run test`
Expected: PASS — 1 teste.

Run: `npm run e2e`
Expected: PASS — 1 teste. (O build leva alguns minutos na primeira execução.)

- [ ] **Step 8: Commit**

```bash
git add vitest.config.ts playwright.config.ts tests package.json package-lock.json .gitignore
git commit -m "chore: toolchain de verificação (Vitest, Playwright, axe-core)"
```

---

### Task 2: Camada de tokens e dark mode

Reestrutura as cores de `globals.css` em duas camadas. Sem o toggle ainda — esta tarefa entrega o dark mode automático por `prefers-color-scheme`.

**Files:**
- Modify: `app/globals.css:9-42` (blocos `@theme inline` e `:root`)
- Modify: `app/globals.css` (15 usos de `var(--ink)`, enumerados no Step 3)
- Create: `tests/unit/contrast.test.ts`
- Create: `lib/contrast.ts`

**Interfaces:**
- Consumes: nada
- Produces: tokens CSS `--brand-{900,800,700,600,500,100,50}`, `--ink-{900,700,500,300,100,50}`, `--paper`, `--urgent`, `--success`, `--warning`, `--info`, e a camada semântica `--surface`, `--surface-raised`, `--surface-alt`, `--surface-inverse`, `--text`, `--text-strong`, `--text-muted`, `--border`, `--accent`. Exporta `lib/contrast.ts` com `contrastRatio(hexA: string, hexB: string): number`.

- [ ] **Step 1: Escrever o teste de contraste**

O briefing exige AAA (≥7:1) no corpo de texto. Este teste trava os pares e falha se alguém escurecer um fundo ou clarear um texto.

`lib/contrast.ts`:

```ts
/** Luminância relativa de uma cor hex, conforme WCAG 2.x. */
export function relativeLuminance(hex: string): number {
  const h = hex.replace('#', '');
  const full = h.length === 3 ? h.split('').map((c) => c + c).join('') : h;
  const [r, g, b] = [0, 2, 4].map((i) => {
    const v = parseInt(full.slice(i, i + 2), 16) / 255;
    return v <= 0.03928 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4;
  });
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}

/** Razão de contraste entre duas cores hex (1 a 21). */
export function contrastRatio(hexA: string, hexB: string): number {
  const a = relativeLuminance(hexA);
  const b = relativeLuminance(hexB);
  const [hi, lo] = a > b ? [a, b] : [b, a];
  return (hi + 0.05) / (lo + 0.05);
}
```

`tests/unit/contrast.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { contrastRatio } from '@/lib/contrast';

const T = {
  paper: '#FFFFFF',
  ink50: '#F4F4F7',
  ink900: '#16161D',
  ink700: '#2A2A35',
  ink500: '#5A5A6B',
  brand700: '#1D1B9A',
  darkSurface: '#101016',
  darkText: '#E8E8EE',
  darkMuted: '#A8A8B8',
  darkAccent: '#8B87F0',
};

describe('contraste dos tokens', () => {
  it('calcula razões conhecidas', () => {
    expect(contrastRatio('#000000', '#FFFFFF')).toBeCloseTo(21, 1);
    expect(contrastRatio('#FFFFFF', '#FFFFFF')).toBeCloseTo(1, 5);
  });

  it('corpo de texto atinge AAA (>= 7:1) no tema claro', () => {
    expect(contrastRatio(T.ink700, T.paper)).toBeGreaterThanOrEqual(7);
    expect(contrastRatio(T.ink700, T.ink50)).toBeGreaterThanOrEqual(7);
  });

  it('texto auxiliar atinge AA (>= 4.5:1) no tema claro', () => {
    expect(contrastRatio(T.ink500, T.paper)).toBeGreaterThanOrEqual(4.5);
    expect(contrastRatio(T.ink500, T.ink50)).toBeGreaterThanOrEqual(4.5);
  });

  it('links de marca atingem AA no tema claro', () => {
    expect(contrastRatio(T.brand700, T.paper)).toBeGreaterThanOrEqual(4.5);
  });

  it('corpo de texto atinge AAA no tema escuro', () => {
    expect(contrastRatio(T.darkText, T.darkSurface)).toBeGreaterThanOrEqual(7);
  });

  it('texto auxiliar e acento atingem AA no tema escuro', () => {
    expect(contrastRatio(T.darkMuted, T.darkSurface)).toBeGreaterThanOrEqual(4.5);
    expect(contrastRatio(T.darkAccent, T.darkSurface)).toBeGreaterThanOrEqual(4.5);
  });
});
```

- [ ] **Step 2: Rodar o teste e verificar que falha**

Run: `npm run test -- contrast`
Expected: FAIL — `Cannot find module '@/lib/contrast'`. Depois de criar `lib/contrast.ts` no Step 1, deve PASSAR; se algum par falhar, o token está errado e precisa ser ajustado antes de seguir.

- [ ] **Step 3: Substituir os blocos de token em `app/globals.css`**

Trocar todo o conteúdo entre `@theme inline {` e o fim do bloco `:root { ... }` (linhas 9-42) por:

```css
@theme inline {
  --font-sans: var(--font-inter), -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
  --font-display: var(--font-archivo), "Segoe UI", Roboto, Arial, sans-serif;
  --color-brand: var(--brand-700);
  --color-brand-deep: var(--brand-800);
  --color-brand-bright: var(--brand-600);
  --color-ink: var(--ink-900);
  --color-surface: var(--surface);
}

/* =========================================================
   Camada 1 — rampas fixas. Não mudam com o tema.
   ========================================================= */
:root {
  --brand-900:#0F0E52; --brand-800:#15137A; --brand-700:#1D1B9A;
  --brand-600:#2B27C4; --brand-500:#3D38E0; --brand-100:#E4E3FB; --brand-50:#F2F1FE;

  --ink-900:#16161D; --ink-700:#2A2A35; --ink-500:#5A5A6B;
  --ink-300:#9A9AAB; --ink-100:#E8E8EE; --ink-50:#F4F4F7;
  --paper:#FFFFFF;

  --urgent:#B3261E; --success:#1B6E3C; --warning:#8A5A00; --info:var(--brand-700);

  /* =======================================================
     Camada 2 — semântica de superfície. É a ÚNICA coisa que
     o dark mode redefine. Nenhuma cor pode nascer dentro de
     um @media ou de um [data-theme].
     ======================================================= */
  --surface:         var(--ink-50);
  --surface-raised:  var(--paper);
  --surface-alt:     var(--ink-100);
  --surface-inverse: var(--ink-900);
  --text:            var(--ink-700);
  --text-strong:     var(--ink-900);
  --text-muted:      var(--ink-500);
  --text-on-inverse: #C6C6D6;
  --border:          var(--ink-100);
  --border-inverse:  #3C3C47;
  --accent:          var(--brand-700);
  --accent-hover:    var(--brand-800);
  --accent-soft:     var(--brand-50);

  /* Apelidos legados: mantêm as ~500 linhas seguintes funcionando
     sem reescrita. Removidos quando o CSS for migrado por completo. */
  --brand:        var(--accent);
  --brand-deep:   var(--accent-hover);
  --brand-bright: var(--brand-600);
  --line:         var(--border);
  --ink-line:     var(--border-inverse);
  --text-light:   var(--text-on-inverse);
  --paper-pure:   var(--surface-raised);
  --paper-alt:    var(--surface-alt);
  --danger:       var(--urgent);

  --radius:     4px;
  --radius-lg:  10px;
  --container:  1140px;
  --gutter:     clamp(1.1rem, 4vw, 2.5rem);
  --font-display-stack: var(--font-display);
  --font-sans-stack: var(--font-sans);
  --shadow:     0 1px 2px rgba(16,16,29,.06);
  --shadow-md:  0 4px 12px rgba(16,16,29,.08);
  --shadow-lg:  0 12px 32px rgba(16,16,29,.10);
  --ease:       cubic-bezier(.22,.61,.36,1);
}

/* Tema escuro automático — só a camada 2, e nunca vence um
   [data-theme="light"] explícito do usuário. */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    --surface:         #101016;
    --surface-raised:  #1D1D27;
    --surface-alt:     #17171F;
    --surface-inverse: #08080C;
    --text:            #E8E8EE;
    --text-strong:     #FFFFFF;
    --text-muted:      #A8A8B8;
    --text-on-inverse: #C6C6D6;
    --border:          #2C2C38;
    --border-inverse:  #33333F;
    --accent:          #8B87F0;
    --accent-hover:    #A8A5F5;
    --accent-soft:     #1A1930;
  }
}

/* Tema escuro explícito — o toggle vence nos dois sentidos. */
:root[data-theme="dark"] {
  --surface:         #101016;
  --surface-raised:  #1D1D27;
  --surface-alt:     #17171F;
  --surface-inverse: #08080C;
  --text:            #E8E8EE;
  --text-strong:     #FFFFFF;
  --text-muted:      #A8A8B8;
  --text-on-inverse: #C6C6D6;
  --border:          #2C2C38;
  --border-inverse:  #33333F;
  --accent:          #8B87F0;
  --accent-hover:    #A8A5F5;
  --accent-soft:     #1A1930;
}
```

- [ ] **Step 4: Desdobrar os 15 usos de `var(--ink)`**

`--ink` acumulava dois papéis: cor de título e fundo de seção escura. No tema claro ambos eram `#26262e` e funcionava; no escuro, título precisa clarear e fundo precisa escurecer. Substituir **exatamente** estas linhas de `app/globals.css`:

Para `var(--surface-inverse)` (era fundo) — linhas 80, 175, 220, 432:

```
.section--ink { background: var(--surface-inverse); ... }
.hero { background: var(--surface-inverse); ... }
.page-hero { background: var(--surface-inverse); ... }
.site-footer { background: var(--surface-inverse); ... }
```

Para `var(--text-strong)` (era cor de texto ou marca de primeiro plano) — linhas 62, 69, 114, 115 (duas ocorrências), 137, 150, 309, 325, 365, 414, 468.

A linha 150 é o traço do botão hambúrguer (`background` de um `<span>` decorativo que representa texto) — vai para `--text-strong` junto com as demais, não para `--surface-inverse`.

Depois, remover a definição órfã `--ink-deep` (0 usos) e a linha `--ink: #26262e;` do bloco antigo.

- [ ] **Step 5: Corrigir os pontos de cor fixa que o dark mode quebraria**

Estas regras têm cor literal e ficariam ilegíveis no escuro. Substituir em `app/globals.css`:

```css
/* .btn-ghost:hover — era rgba(38,38,46,.04) */
.btn-ghost:hover { border-color: var(--text-strong); color: var(--text-strong); background: color-mix(in srgb, var(--text-strong) 5%, transparent); }

/* .nav a:hover — era rgba(29,27,154,.06) */
.nav a:not(.btn):hover { background: var(--accent-soft); color: var(--accent); }

/* .site-header — era rgba(255,255,255,.93) */
.site-header { background: color-mix(in srgb, var(--surface) 93%, transparent); }
.site-header.is-scrolled { background: color-mix(in srgb, var(--surface) 98%, transparent); box-shadow: var(--shadow-md); }

/* .area-card .card-icon — era rgba(29,27,154,.07) */
.area-card .card-icon { background: var(--accent-soft); }

/* .founder-body .oab — era rgba(29,27,154,.08) / rgba(29,27,154,.2) */
.founder-body .oab { color: var(--accent); background: var(--accent-soft); border-color: color-mix(in srgb, var(--accent) 20%, transparent); }

/* .post-tag — era rgba(29,27,154,.07) / rgba(29,27,154,.18) */
.post-tag { color: var(--accent); background: var(--accent-soft); border-color: color-mix(in srgb, var(--accent) 18%, transparent); }

/* .field input:focus — era rgba(29,27,154,.14) */
.field input:focus, .field select:focus, .field textarea:focus { box-shadow: 0 0 0 3px color-mix(in srgb, var(--accent) 18%, transparent); }

/* .field input::placeholder — era #9a9aa3 */
.field input::placeholder, .field textarea::placeholder { color: var(--ink-300); }

/* .form-feedback — era #eef0fb / #bfbef0 / var(--brand-deep) */
.form-feedback { background: var(--accent-soft); border-color: color-mix(in srgb, var(--accent) 35%, transparent); color: var(--text); }
.form-feedback h3 { color: var(--text-strong); }

/* .form-error — era #fbeceb / #e2b4af / #8c2f26 */
.form-error { background: color-mix(in srgb, var(--urgent) 10%, var(--surface-raised)); border-color: color-mix(in srgb, var(--urgent) 40%, transparent); color: var(--urgent); }
.form-error a { color: var(--urgent); }
```

Também declarar o esquema no `body` para que os controles nativos (scrollbar, inputs) acompanhem o tema:

```css
:root { color-scheme: light; }
@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) { color-scheme: dark; } }
:root[data-theme="dark"] { color-scheme: dark; }
:root[data-theme="light"] { color-scheme: light; }
```

- [ ] **Step 6: Rodar os testes e conferir visualmente**

Run: `npm run test`
Expected: PASS — todos os testes de contraste.

Run: `npm run dev -- --port 3100`, abrir `http://localhost:3100` e alternar o tema do sistema operacional entre claro e escuro. Percorrer `/`, `/areas`, `/sobre`, `/blog`, `/contato`, `/diagnostico`. Nenhum texto pode ficar ilegível, nenhuma seção pode ficar branca no escuro.

- [ ] **Step 7: Commit**

```bash
git add app/globals.css lib/contrast.ts tests/unit/contrast.test.ts
git commit -m "feat: tokens em duas camadas e dark mode automático"
```

---

### Task 3: `bootstrap.js` — tema sem FOUC e toggle

O script de mesma origem que substitui o inline. É a peça que destrava o CSP da Tarefa 9.

**Files:**
- Create: `public/bootstrap.js`
- Create: `components/theme-toggle.tsx`
- Modify: `app/layout.tsx`
- Modify: `components/site-header.tsx`
- Modify: `app/globals.css` (estilo do toggle)
- Create: `tests/e2e/theme.spec.ts`

**Interfaces:**
- Consumes: tokens `[data-theme]` da Tarefa 2
- Produces: `public/bootstrap.js` lê `localStorage['dsa-theme']` e escreve `data-theme` em `<html>` antes do paint; `<ThemeToggle />` exportado de `components/theme-toggle.tsx`

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/theme.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('respeita o tema escuro do sistema sem escolha explícita', async ({ browser }) => {
  const ctx = await browser.newContext({ colorScheme: 'dark' });
  const page = await ctx.newPage();
  await page.goto('/');
  const bg = await page.evaluate(() =>
    getComputedStyle(document.body).backgroundColor
  );
  // #101016
  expect(bg).toBe('rgb(16, 16, 22)');
  await ctx.close();
});

test('o toggle sobrepõe o sistema e persiste entre navegações', async ({ browser }) => {
  const ctx = await browser.newContext({ colorScheme: 'dark' });
  const page = await ctx.newPage();
  await page.goto('/');
  await page.getByRole('button', { name: /tema/i }).click();
  await expect(page.locator('html')).toHaveAttribute('data-theme', 'light');

  await page.goto('/areas');
  await expect(page.locator('html')).toHaveAttribute('data-theme', 'light');
});

test('aplica o tema antes do primeiro paint (sem FOUC)', async ({ browser }) => {
  const ctx = await browser.newContext({ colorScheme: 'light' });
  const page = await ctx.newPage();
  await page.addInitScript(() => {
    try { localStorage.setItem('dsa-theme', 'dark'); } catch {}
  });
  await page.goto('/', { waitUntil: 'commit' });
  // Já no primeiro documento, antes de qualquer hidratação.
  await expect(page.locator('html')).toHaveAttribute('data-theme', 'dark');
  await ctx.close();
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- theme`
Expected: FAIL — o botão de tema não existe e `data-theme` nunca é definido.

- [ ] **Step 3: Criar `public/bootstrap.js`**

Script clássico, sem módulos, sem dependências. Roda bloqueante no `<head>`. A parte de reveal entra na Tarefa 4 — aqui só o tema.

```js
/* Bootstrap de mesma origem. Roda antes do primeiro paint.
   Existe para que o site não precise de nenhum <script> inline,
   o que permite um CSP com script-src 'self' e sem nonce. */
(function () {
  var root = document.documentElement;

  try {
    var saved = localStorage.getItem('dsa-theme');
    if (saved === 'dark' || saved === 'light') {
      root.setAttribute('data-theme', saved);
    }
  } catch (e) {
    /* modo privado ou storage bloqueado: segue o prefers-color-scheme */
  }
})();
```

- [ ] **Step 4: Carregar o bootstrap em `app/layout.tsx`**

No `<head>`, substituindo os três `<link>` de Google Fonts (que saem na Tarefa 5; por ora apenas acrescentar o script antes deles):

```tsx
<script src="/bootstrap.js" />
```

Em React 19 um `<script src>` sem `async` renderizado no `<head>` é emitido como tag bloqueante. Não usar `next/script` aqui: as estratégias dele injetam o script depois da hidratação, o que reintroduz o FOUC.

- [ ] **Step 5: Criar `components/theme-toggle.tsx`**

```tsx
'use client';

import { useEffect, useState } from 'react';
import { Moon, Sun } from 'lucide-react';

type Theme = 'light' | 'dark';

/**
 * Alterna o tema e persiste a escolha. O estado inicial é lido do DOM
 * (que o /bootstrap.js já resolveu antes do paint), nunca do localStorage
 * durante a renderização — isso evita divergência de hidratação.
 */
export function ThemeToggle() {
  const [theme, setTheme] = useState<Theme | null>(null);

  useEffect(() => {
    const attr = document.documentElement.getAttribute('data-theme');
    if (attr === 'dark' || attr === 'light') {
      setTheme(attr);
      return;
    }
    setTheme(
      window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
    );
  }, []);

  function toggle() {
    const next: Theme = theme === 'dark' ? 'light' : 'dark';
    document.documentElement.setAttribute('data-theme', next);
    try {
      localStorage.setItem('dsa-theme', next);
    } catch {
      /* sem persistência em modo privado — o tema vale para esta sessão */
    }
    setTheme(next);
  }

  const label =
    theme === 'dark' ? 'Mudar para o tema claro' : 'Mudar para o tema escuro';

  return (
    <button
      type="button"
      className="theme-toggle"
      onClick={toggle}
      aria-label={label}
      title={label}
    >
      {theme === 'dark' ? (
        <Sun size={20} strokeWidth={1.5} aria-hidden="true" />
      ) : (
        <Moon size={20} strokeWidth={1.5} aria-hidden="true" />
      )}
    </button>
  );
}
```

Instalar o Lucide agora, já que a Tarefa 10 também depende dele:

```bash
npm i lucide-react
```

- [ ] **Step 6: Montar o toggle no header**

Em `components/site-header.tsx`, importar `ThemeToggle` e inseri-lo dentro de `<nav>`, imediatamente antes do `<Link className="btn btn-primary header-cta">`.

- [ ] **Step 7: Estilizar o toggle**

Acrescentar em `app/globals.css`, junto do bloco `/* ---------- Header ---------- */`:

```css
.theme-toggle {
  width: 44px; height: 44px; flex: none;
  display: inline-flex; align-items: center; justify-content: center;
  border: 1px solid var(--border); border-radius: var(--radius);
  background: transparent; color: var(--text); cursor: pointer;
  transition: border-color .16s var(--ease), background .16s var(--ease);
}
.theme-toggle:hover { border-color: var(--text-strong); background: var(--surface-alt); }
```

O alvo de 44×44px atende o WCAG 2.5.8.

- [ ] **Step 8: Rodar e verificar que passa**

Run: `npm run e2e -- theme`
Expected: PASS — 3 testes.

- [ ] **Step 9: Commit**

```bash
git add public/bootstrap.js components/theme-toggle.tsx components/site-header.tsx app/layout.tsx app/globals.css tests/e2e/theme.spec.ts package.json package-lock.json
git commit -m "feat: bootstrap de mesma origem e alternador de tema sem FOUC"
```

---

### Task 4: P1 — reveal reescrito

O defeito bloqueante. Ao final, `/areas` mostra os 14 cards com JavaScript desligado.

**Files:**
- Modify: `components/reveal.tsx` (reescrita completa — deixa de ser client component)
- Modify: `public/bootstrap.js`
- Modify: `app/globals.css:563-566` (remover o CSS órfão) e bloco novo de reveal
- Modify: `app/(site)/page.tsx` (props do `Reveal`, remover reveal acima da dobra)
- Modify: `app/(site)/areas/page.tsx` (índice do stagger)
- Create: `tests/e2e/reveal.spec.ts`

**Interfaces:**
- Consumes: `.js` em `<html>`, posto por `public/bootstrap.js` (Tarefa 3)
- Produces: `<Reveal as? className? index?>`, `<RevealGroup as? className?>`, `<RevealItem index className?>` — todos **server components**, sem `'use client'`. `index` é um número; o componente aplica `min(index, 5)`.

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/reveal.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('sem JavaScript, /areas mostra os 14 cards', async ({ browser }) => {
  const ctx = await browser.newContext({ javaScriptEnabled: false });
  const page = await ctx.newPage();
  await page.goto('/areas');

  const cards = page.locator('a.area-card');
  await expect(cards).toHaveCount(14);

  // Não basta existir no DOM: precisa estar visível e opaco.
  const opacities = await cards.evaluateAll((els) =>
    els.map((el) => getComputedStyle(el).opacity)
  );
  expect(opacities.every((o) => Number(o) === 1)).toBe(true);
  await ctx.close();
});

test('sem JavaScript, a home mostra o corpo das seções', async ({ browser }) => {
  const ctx = await browser.newContext({ javaScriptEnabled: false });
  const page = await ctx.newPage();
  await page.goto('/');
  await expect(
    page.getByRole('heading', { name: /escritório-boutique com raiz/i })
  ).toBeVisible();
  await ctx.close();
});

test('com JavaScript, nada fica invisível depois de entrar no viewport', async ({ page }) => {
  await page.goto('/areas');
  await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
  await page.waitForTimeout(400);

  const hidden = await page.evaluate(() =>
    Array.from(document.querySelectorAll('.reveal')).filter((el) => {
      const r = el.getBoundingClientRect();
      const dentro = r.top < window.innerHeight && r.bottom > 0;
      return dentro && Number(getComputedStyle(el).opacity) < 1;
    }).length
  );
  expect(hidden).toBe(0);
});

test('o CSS órfão que apagava elementos não existe mais', async ({ page }) => {
  await page.goto('/');
  const marcado = await page.evaluate(() => {
    const el = document.createElement('div');
    el.setAttribute('data-reveal', '');
    el.textContent = 'teste';
    document.body.appendChild(el);
    const o = getComputedStyle(el).opacity;
    el.remove();
    return o;
  });
  expect(Number(marcado)).toBe(1);
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- reveal`
Expected: FAIL — o primeiro teste encontra 14 cards com `opacity: 0`, exatamente o defeito P1.

- [ ] **Step 3: Reescrever `components/reveal.tsx`**

Sai o Motion. O componente passa a ser server-side e só marca o elemento.

```tsx
import type { CSSProperties, ElementType, ReactNode } from 'react';

/** Stagger de no máximo 6 itens (0..5), conforme §4.1 do briefing. */
const MAX_STAGGER = 5;

type RevealProps = {
  children: ReactNode;
  className?: string;
  as?: ElementType;
  /** Posição no lote. Acima de 5 o atraso satura — 14 cards nunca fazem cascata. */
  index?: number;
};

function revealStyle(index: number): CSSProperties {
  return { '--reveal-i': Math.min(index, MAX_STAGGER) } as CSSProperties;
}

/**
 * Marca um bloco para entrada suave. O estado padrão no CSS é VISÍVEL:
 * a ocultação só existe sob `html.js`, adicionada por /bootstrap.js.
 * Sem JavaScript, nada aqui esconde conteúdo.
 */
export function Reveal({
  children,
  className,
  as: Tag = 'div',
  index = 0,
}: RevealProps) {
  return (
    <Tag
      className={className ? `${className} reveal` : 'reveal'}
      style={revealStyle(index)}
    >
      {children}
    </Tag>
  );
}

/** Contêiner de um lote. Não anima por si — apenas agrupa. */
export function RevealGroup({
  children,
  className,
  as: Tag = 'div',
}: {
  children: ReactNode;
  className?: string;
  as?: ElementType;
}) {
  return <Tag className={className}>{children}</Tag>;
}

/** Item de um lote. O chamador passa o índice para escalonar a entrada. */
export function RevealItem({
  children,
  className,
  as: Tag = 'div',
  index = 0,
}: RevealProps) {
  return (
    <Tag
      className={className ? `${className} reveal` : 'reveal'}
      style={revealStyle(index)}
    >
      {children}
    </Tag>
  );
}
```

- [ ] **Step 4: Substituir o CSS de reveal**

Remover as linhas 563-566 de `app/globals.css` (o bloco `[data-reveal]` órfão) e a regra `[data-reveal]` dentro do `@media (prefers-reduced-motion: reduce)`. No lugar:

```css
/* ---------- Reveal on scroll ----------
   O estado padrão é VISÍVEL. Só `html.js` — posta por /bootstrap.js antes
   do primeiro paint — autoriza a ocultação. Sem JS, nada aqui esconde nada. */
@keyframes reveal-in {
  from { opacity: 0; transform: translateY(12px); }
  to   { opacity: 1; transform: none; }
}

.js .reveal { opacity: 0; transform: translateY(12px); }
.js .reveal.is-in {
  opacity: 1; transform: none;
  transition: opacity 320ms var(--ease), transform 320ms var(--ease);
  transition-delay: calc(var(--reveal-i, 0) * 40ms);
}

/* Caminho primário: animação dirigida pela rolagem, sem JS na main thread.
   O stagger vira espacial — cada item entra 4% de viewport após o anterior. */
@supports (animation-timeline: view()) {
  .js .reveal {
    animation: reveal-in 320ms var(--ease) both;
    animation-timeline: view();
    animation-range: entry calc(var(--reveal-i, 0) * 4%)
                     entry calc(30% + var(--reveal-i, 0) * 4%);
  }
}

/* Rede de segurança: se o bootstrap adicionou `.js` mas a inicialização
   não completou, tudo volta a ser visível. */
.reveal-done .reveal {
  opacity: 1 !important;
  transform: none !important;
  animation: none !important;
}
```

E dentro do bloco `@media (prefers-reduced-motion: reduce)`:

```css
  .js .reveal { opacity: 1 !important; transform: none !important; animation: none !important; }
```

- [ ] **Step 5: Acrescentar o runtime de reveal ao `public/bootstrap.js`**

Dentro do mesmo IIFE, depois do bloco de tema:

```js
  /* A partir daqui o CSS pode esconder. Antes disso, não podia. */
  root.classList.add('js');

  var pronto = false;
  /* Se a inicialização não completar, o conteúdo volta a aparecer.
     Só dispara quando `pronto` continua falso — uma navegação normal
     nunca perde a animação por causa deste timeout. */
  setTimeout(function () {
    if (!pronto) root.classList.add('reveal-done');
  }, 1200);

  try {
    var temTimeline =
      window.CSS && CSS.supports && CSS.supports('animation-timeline', 'view()');

    if (temTimeline) {
      /* Caminho CSS puro: nada a fazer. */
      pronto = true;
      return;
    }

    if (!('IntersectionObserver' in window)) {
      root.classList.add('reveal-done');
      pronto = true;
      return;
    }

    var io = new IntersectionObserver(
      function (entries) {
        for (var i = 0; i < entries.length; i++) {
          if (entries[i].isIntersecting) {
            entries[i].target.classList.add('is-in');
            io.unobserve(entries[i].target);
          }
        }
      },
      { rootMargin: '0px 0px -8% 0px', threshold: 0.05 }
    );

    var observar = function () {
      var els = document.querySelectorAll('.reveal:not(.is-in)');
      for (var i = 0; i < els.length; i++) io.observe(els[i]);
    };

    var agendado = false;
    var agendar = function () {
      if (agendado) return;
      agendado = true;
      requestAnimationFrame(function () {
        agendado = false;
        observar();
      });
    };

    var iniciar = function () {
      observar();
      /* Navegação client-side do App Router injeta novos .reveal. */
      new MutationObserver(agendar).observe(document.body, {
        childList: true,
        subtree: true,
      });
      pronto = true;
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', iniciar);
    } else {
      iniciar();
    }
  } catch (e) {
    root.classList.add('reveal-done');
    pronto = true;
  }
```

- [ ] **Step 6: Tirar o reveal de cima da dobra e ajustar os índices**

Em `app/(site)/page.tsx`:
- Trocar `<Reveal className="aside-card" delay={0.1}>` por `<Reveal className="aside-card" index={1}>` — a prop `delay` não existe mais.
- Nos dois `RevealGroup` de áreas, passar o índice: `{areasPublico.map((area, i) => (<RevealItem key={area.slug} index={i}>`), e o mesmo para `areasCivel`.

Em `app/(site)/areas/page.tsx`: mesma mudança nos dois `RevealGroup`.

Conferir que nenhum `Reveal` envolve o hero ou o primeiro parágrafo de qualquer página. Na home o hero já está fora; em `/areas/[slug]` o único `Reveal` está na seção de FAQ, bem abaixo da dobra — mantém.

- [ ] **Step 7: Rodar e verificar que passa**

Run: `npm run e2e -- reveal`
Expected: PASS — 4 testes. O primeiro é o critério de aceite nº 1 do briefing.

Run: `npm run e2e`
Expected: PASS — nenhuma regressão nos testes anteriores.

- [ ] **Step 8: Commit**

```bash
git add components/reveal.tsx public/bootstrap.js app/globals.css "app/(site)/page.tsx" "app/(site)/areas/page.tsx" tests/e2e/reveal.spec.ts
git commit -m "fix: conteúdo deixa de depender de JavaScript para ficar visível (P1)"
```

---

### Task 5: Fontes self-hosted e micro-tipografia

Tira o caminho crítico das mãos do Google e aplica as correções do §3.2.

**Files:**
- Create: `lib/fonts.ts`
- Modify: `app/layout.tsx`
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: `--font-inter` / `--font-archivo` já referenciados no `@theme` da Tarefa 2
- Produces: `lib/fonts.ts` exporta `archivo` e `inter` (objetos de `next/font/google` com `variable` definida)

- [ ] **Step 1: Criar `lib/fonts.ts`**

`next/font/google` baixa os arquivos no build e os serve do próprio domínio — não há requisição a `fonts.googleapis.com` em produção. É o que satisfaz o "self-hosted" do briefing sem gerir arquivos `.woff2` à mão.

```ts
import { Archivo, Inter } from 'next/font/google';

export const archivo = Archivo({
  subsets: ['latin', 'latin-ext'],
  weight: ['600', '700'],
  display: 'swap',
  variable: '--font-archivo',
});

export const inter = Inter({
  subsets: ['latin', 'latin-ext'],
  weight: ['400', '500', '600', '700'],
  display: 'swap',
  variable: '--font-inter',
});
```

- [ ] **Step 2: Aplicar em `app/layout.tsx`**

Remover os três `<link>` de Google Fonts do `<head>` — inclusive os dois `preconnect`, que ficam inúteis. Aplicar as variáveis no `<html>`:

```tsx
import { archivo, inter } from '@/lib/fonts';
// ...
<html lang="pt-BR" className={`${archivo.variable} ${inter.variable}`}>
  <head>
    <script src="/bootstrap.js" />
  </head>
  <body>{children}</body>
</html>
```

- [ ] **Step 3: Aplicar a escala e as correções de micro-tipografia**

Substituir o bloco de títulos e parágrafos em `app/globals.css` (linhas 61-67):

```css
h1, h2, h3, h4 {
  font-family: var(--font-display-stack); font-weight: 700; color: var(--text-strong);
  margin: 0 0 .6em; text-wrap: balance;
}
h1 { font-size: clamp(2.5rem, 5vw, 4rem);      line-height: 1.05; letter-spacing: -.02em; }
h2 { font-size: clamp(1.9rem, 3.5vw, 2.75rem); line-height: 1.15; letter-spacing: -.02em; }
h3 { font-size: clamp(1.25rem, 2vw, 1.5rem);   line-height: 1.3;  letter-spacing: -.01em; }
p  { margin: 0 0 1.1em; text-wrap: pretty; }
```

E acrescentar, junto ao reset:

```css
html { -webkit-text-size-adjust: 100%; hyphens: auto; }
/* Números de etapa, datas e prazos alinham em coluna. */
.steps li::before, .article-meta time, .post-meta, .hero-meta dt, [data-prazo] {
  font-variant-numeric: tabular-nums;
}
```

Corrigir o eyebrow que quebrava em duas linhas em `/modelos`:

```css
.eyebrow { ... text-wrap: balance; }
@media (max-width: 1023px) { .eyebrow { letter-spacing: .08em; } }
```

E a medida de leitura do artigo:

```css
.prose { max-width: 68ch; }
```

- [ ] **Step 4: Verificar**

Run: `npm run build`
Expected: build conclui sem erro.

Run: `npm run e2e`
Expected: PASS — todos os testes anteriores.

Abrir `/modelos` em 768px e confirmar que nenhum eyebrow quebra em duas linhas. Abrir um artigo do blog e confirmar que a linha de texto tem entre 65 e 72 caracteres.

Confirmar na aba Network do navegador, em produção local (`npm run build && npm run start`), que **não há** requisição para `fonts.googleapis.com` nem `fonts.gstatic.com`.

- [ ] **Step 5: Commit**

```bash
git add lib/fonts.ts app/layout.tsx app/globals.css
git commit -m "perf: fontes self-hosted e correções de micro-tipografia"
```

---

### Task 6: P2 — hero leve

**Files:**
- Modify: `public/assets/video/hero.mp4` (reencodado)
- Create: `public/assets/video/hero.webm`
- Modify: `components/hero-video.tsx`
- Modify: `app/globals.css` (botão de pausa e crossfade)
- Create: `tests/e2e/orcamento.spec.ts`

**Interfaces:**
- Consumes: nada
- Produces: `<HeroVideo />` (client component) que renderiza o poster como LCP e monta o vídeo apenas sob as cinco condições do §4.2

- [ ] **Step 1: Escrever o teste de orçamento**

`tests/e2e/orcamento.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('a home cabe em 900 KB e nenhum asset passa de 600 KB', async ({ page }) => {
  const porUrl = new Map<string, number>();

  page.on('response', async (res) => {
    const url = res.url();
    // Não fixar a porta: o filtro precisa acompanhar o baseURL da config.
    // Um número de porta desatualizado aqui faria o teste passar por acidente,
    // porque `porUrl` ficaria vazio e o total daria zero.
    if (!url.startsWith('http://localhost:')) return;
    try {
      const buf = await res.body();
      porUrl.set(url, buf.length);
    } catch {
      /* respostas sem corpo (redirects, 304) */
    }
  });

  await page.goto('/', { waitUntil: 'networkidle' });

  const total = [...porUrl.values()].reduce((a, b) => a + b, 0);
  const [maiorUrl, maiorBytes] = [...porUrl.entries()].sort((a, b) => b[1] - a[1])[0] ?? ['nenhum', 0];

  expect(
    total,
    `total ${(total / 1024).toFixed(0)} KB; maior: ${maiorUrl} (${(maiorBytes / 1024).toFixed(0)} KB)`
  ).toBeLessThanOrEqual(900 * 1024);

  for (const [url, bytes] of porUrl) {
    expect(bytes, `${url} tem ${(bytes / 1024).toFixed(0)} KB`).toBeLessThanOrEqual(600 * 1024);
  }
});
```

O teste roda em viewport desktop padrão do Playwright (1280×720), abaixo do gate de 1024px? Não — 1280 ≥ 1024. Mas `navigator.connection` não existe no Chromium headless por padrão, e a regra do §4.2 é conservadora: sem `connection`, o vídeo não carrega. O teste, portanto, mede a home **sem** o vídeo, que é o caso da maioria dos visitantes móveis. Um segundo teste cobre o caso com vídeo:

```ts
test('o vídeo do hero cabe em 600 KB', async ({ request }) => {
  for (const arquivo of ['/assets/video/hero.webm', '/assets/video/hero.mp4']) {
    const res = await request.get(arquivo);
    expect(res.status()).toBe(200);
    const bytes = (await res.body()).length;
    expect(bytes, `${arquivo} tem ${(bytes / 1024).toFixed(0)} KB`).toBeLessThanOrEqual(600 * 1024);
  }
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- orcamento`
Expected: FAIL — `hero.webm` não existe (404) e `hero.mp4` tem 2,5 MB.

- [ ] **Step 3: Reencodar o vídeo**

Guardar o original fora do repositório antes de sobrescrever:

```bash
mkdir -p /tmp/dsa && cp public/assets/video/hero.mp4 /tmp/dsa/hero-original.mp4

# Confirmar quais encoders AV1 o ffmpeg local tem
ffmpeg -hide_banner -encoders | grep -Ei 'av1|vp9'
```

WebM primário (usar `libsvtav1`; se ausente, trocar por `-c:v libvpx-vp9 -b:v 0 -crf 40 -row-mt 1`):

```bash
ffmpeg -y -i /tmp/dsa/hero-original.mp4 -t 8 -an \
  -vf "scale=608:1080:flags=lanczos" \
  -c:v libsvtav1 -crf 45 -preset 6 -g 240 -pix_fmt yuv420p \
  public/assets/video/hero.webm
```

MP4 de fallback:

```bash
ffmpeg -y -i /tmp/dsa/hero-original.mp4 -t 8 -an \
  -vf "scale=608:1080:flags=lanczos" \
  -c:v libx264 -crf 30 -preset slow -profile:v high -pix_fmt yuv420p \
  -movflags +faststart \
  public/assets/video/hero.mp4
```

Medir:

```bash
ls -l public/assets/video/
```

Se algum passar de 600 KB, subir o CRF de 3 em 3 (AV1 até 50, H.264 até 34) e reencodar. **Registrar os tamanhos finais reais** na mensagem de commit — não presumir.

- [ ] **Step 4: Reescrever `components/hero-video.tsx`**

```tsx
'use client';

import Image from 'next/image';
import { useEffect, useRef, useState } from 'react';
import { Pause, Play } from 'lucide-react';

/** As cinco condições do §4.2. Falta uma, o vídeo não carrega. */
function podeCarregarVideo(): boolean {
  if (typeof window === 'undefined') return false;
  if (!window.matchMedia('(min-width: 1024px)').matches) return false;
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return false;

  const conn = (
    navigator as Navigator & {
      connection?: { effectiveType?: string; saveData?: boolean };
    }
  ).connection;

  // Sem informação de rede, a escolha conservadora é não baixar 600 KB.
  if (!conn) return false;
  if (conn.saveData === true) return false;
  return conn.effectiveType === '4g';
}

/**
 * Hero: o poster é sempre o LCP e nunca depende de JavaScript. O vídeo é
 * uma camada opcional, montada só em desktop, em 4G, sem economia de dados
 * e sem preferência por movimento reduzido.
 */
export function HeroVideo() {
  const hostRef = useRef<HTMLDivElement>(null);
  const videoRef = useRef<HTMLVideoElement>(null);
  const [montar, setMontar] = useState(false);
  const [visivel, setVisivel] = useState(false);
  const [pausado, setPausado] = useState(false);

  useEffect(() => {
    if (!podeCarregarVideo()) return;
    const host = hostRef.current;
    if (!host) return;

    const io = new IntersectionObserver(
      (entries) => {
        if (entries.some((e) => e.isIntersecting)) {
          setMontar(true);
          io.disconnect();
        }
      },
      { threshold: 0.1 }
    );
    io.observe(host);
    return () => io.disconnect();
  }, []);

  function alternar() {
    const v = videoRef.current;
    if (!v) return;
    if (v.paused) {
      void v.play();
      setPausado(false);
    } else {
      v.pause();
      setPausado(true);
    }
  }

  return (
    <div className="hero-media" ref={hostRef}>
      <Image
        src="/assets/img/douglas-retrato.jpg"
        alt="Douglas Senturião, advogado responsável pelo escritório"
        width={1100}
        height={1650}
        sizes="(min-width: 940px) 46vw, 92vw"
        priority
      />

      {montar && (
        <>
          <video
            ref={videoRef}
            className={visivel ? 'hero-video is-visible' : 'hero-video'}
            autoPlay
            muted
            loop
            playsInline
            preload="none"
            aria-hidden="true"
            tabIndex={-1}
            onCanPlay={() => setVisivel(true)}
          >
            <source src="/assets/video/hero.webm" type="video/webm" />
            <source src="/assets/video/hero.mp4" type="video/mp4" />
          </video>
          {visivel && (
            <button
              type="button"
              className="hero-video-toggle"
              onClick={alternar}
              aria-pressed={pausado}
              aria-label={
                pausado
                  ? 'Retomar o vídeo de fundo'
                  : 'Pausar o vídeo de fundo'
              }
            >
              {pausado ? (
                <Play size={18} strokeWidth={1.5} aria-hidden="true" />
              ) : (
                <Pause size={18} strokeWidth={1.5} aria-hidden="true" />
              )}
            </button>
          )}
        </>
      )}
    </div>
  );
}
```

O vídeo é `aria-hidden` e `tabIndex={-1}`: é decoração, e a informação está no poster. O botão de pausa, ao contrário, é focável e rotulado — WCAG 2.2.2 é obrigatório porque o clipe passa de 5 s.

- [ ] **Step 5: Estilizar a camada de vídeo**

Acrescentar em `app/globals.css`, junto ao bloco `.hero-photo`:

```css
.hero-media { position: relative; z-index: 1; }
.hero-media img { border-radius: var(--radius-lg); }
.hero-video {
  position: absolute; inset: 0; width: 100%; height: 100%;
  object-fit: cover; border-radius: var(--radius-lg);
  opacity: 0; transition: opacity 240ms var(--ease);
}
.hero-video.is-visible { opacity: 1; }
.hero-video-toggle {
  position: absolute; right: 12px; bottom: 12px; z-index: 2;
  width: 44px; height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  border: 1px solid rgba(255,255,255,.4); border-radius: 50%;
  background: rgba(8,8,12,.55); color: #fff; cursor: pointer;
  backdrop-filter: blur(4px);
}
.hero-video-toggle:hover { background: rgba(8,8,12,.75); }
@media (prefers-reduced-motion: reduce) { .hero-video { display: none; } }
```

- [ ] **Step 6: Rodar e verificar que passa**

Run: `npm run e2e -- orcamento`
Expected: PASS — 2 testes.

Run: `npm run e2e`
Expected: PASS — tudo.

- [ ] **Step 7: Commit**

Substituir `<TAMANHOS>` pelos valores medidos no Step 3.

```bash
git add public/assets/video components/hero-video.tsx app/globals.css tests/e2e/orcamento.spec.ts
git commit -m "perf: hero de 2,5 MB para <TAMANHOS> com poster como LCP (P2)"
```

---

### Task 7: Consentimento

Precede o analytics: nenhum script de medição carrega antes da escolha do usuário.

**Files:**
- Create: `lib/consent.ts`
- Create: `components/consent-banner.tsx`
- Create: `tests/unit/consent.test.ts`
- Create: `tests/e2e/consentimento.spec.ts`
- Modify: `app/(site)/layout.tsx`
- Modify: `components/site-footer.tsx`
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: nada
- Produces:
  - `type ConsentState = { v: 1; analytics: boolean; marketing: boolean; ts: number }`
  - `CONSENT_KEY = 'dsa-consent'`
  - `CONSENT_EVENT = 'dsa-consent-change'`
  - `parseConsent(raw: string | null): ConsentState | null`
  - `readConsent(): ConsentState | null`
  - `writeConsent(escolha: { analytics: boolean; marketing: boolean }): ConsentState`
  - `<ConsentBanner />`

- [ ] **Step 1: Escrever os testes unitários**

`tests/unit/consent.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import {
  CONSENT_KEY,
  parseConsent,
  readConsent,
  writeConsent,
} from '@/lib/consent';

describe('parseConsent', () => {
  it('devolve null para ausência de valor', () => {
    expect(parseConsent(null)).toBeNull();
  });

  it('devolve null para JSON inválido', () => {
    expect(parseConsent('{oops')).toBeNull();
  });

  it('devolve null para versão desconhecida — força nova escolha', () => {
    expect(parseConsent(JSON.stringify({ v: 99, analytics: true, marketing: true, ts: 1 }))).toBeNull();
  });

  it('devolve null quando um campo não é booleano', () => {
    expect(parseConsent(JSON.stringify({ v: 1, analytics: 'sim', marketing: false, ts: 1 }))).toBeNull();
  });

  it('aceita um estado válido', () => {
    const estado = { v: 1, analytics: true, marketing: false, ts: 1735689600000 };
    expect(parseConsent(JSON.stringify(estado))).toEqual(estado);
  });
});

describe('readConsent / writeConsent', () => {
  beforeEach(() => localStorage.clear());

  it('sem escolha prévia, não há consentimento', () => {
    expect(readConsent()).toBeNull();
  });

  it('grava e relê a escolha', () => {
    const gravado = writeConsent({ analytics: true, marketing: false });
    expect(gravado.analytics).toBe(true);
    expect(gravado.marketing).toBe(false);
    expect(readConsent()).toEqual(gravado);
  });

  it('a recusa é um estado gravado, não a ausência de estado', () => {
    writeConsent({ analytics: false, marketing: false });
    const lido = readConsent();
    expect(lido).not.toBeNull();
    expect(lido?.analytics).toBe(false);
  });

  it('sobrevive a localStorage indisponível', () => {
    const original = Object.getOwnPropertyDescriptor(window, 'localStorage');
    Object.defineProperty(window, 'localStorage', {
      configurable: true,
      get() { throw new Error('bloqueado'); },
    });
    expect(() => readConsent()).not.toThrow();
    expect(readConsent()).toBeNull();
    if (original) Object.defineProperty(window, 'localStorage', original);
  });
});

describe('chave de armazenamento', () => {
  it('é estável', () => {
    expect(CONSENT_KEY).toBe('dsa-consent');
  });
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run test -- consent`
Expected: FAIL — `Cannot find module '@/lib/consent'`.

- [ ] **Step 3: Implementar `lib/consent.ts`**

```ts
export const CONSENT_KEY = 'dsa-consent';
/** Evento de janela emitido quando a escolha muda, para os carregadores reagirem. */
export const CONSENT_EVENT = 'dsa-consent-change';

const VERSAO = 1 as const;

export type ConsentState = {
  v: typeof VERSAO;
  analytics: boolean;
  marketing: boolean;
  ts: number;
};

/** Valida e desserializa. Qualquer coisa fora do formato vira null — o que
 *  significa "ainda não escolheu" e faz o banner reaparecer. */
export function parseConsent(raw: string | null): ConsentState | null {
  if (!raw) return null;
  try {
    const o = JSON.parse(raw) as Record<string, unknown>;
    if (o.v !== VERSAO) return null;
    if (typeof o.analytics !== 'boolean') return null;
    if (typeof o.marketing !== 'boolean') return null;
    if (typeof o.ts !== 'number') return null;
    return { v: VERSAO, analytics: o.analytics, marketing: o.marketing, ts: o.ts };
  } catch {
    return null;
  }
}

export function readConsent(): ConsentState | null {
  if (typeof window === 'undefined') return null;
  try {
    return parseConsent(window.localStorage.getItem(CONSENT_KEY));
  } catch {
    return null;
  }
}

export function writeConsent(escolha: {
  analytics: boolean;
  marketing: boolean;
}): ConsentState {
  const estado: ConsentState = { v: VERSAO, ...escolha, ts: Date.now() };
  try {
    window.localStorage.setItem(CONSENT_KEY, JSON.stringify(estado));
  } catch {
    /* sem persistência: a escolha vale para esta sessão */
  }
  window.dispatchEvent(new CustomEvent(CONSENT_EVENT, { detail: estado }));
  return estado;
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm run test -- consent`
Expected: PASS — 9 testes.

- [ ] **Step 5: Escrever o teste E2E do banner**

`tests/e2e/consentimento.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('o banner aparece na primeira visita', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByRole('dialog', { name: /privacidade/i })).toBeVisible();
});

test('"Recusar" e "Aceitar" têm o mesmo peso visual', async ({ page }) => {
  await page.goto('/');
  const aceitar = page.getByRole('button', { name: 'Aceitar todos' });
  const recusar = page.getByRole('button', { name: 'Recusar todos' });

  const [ca, cr] = await Promise.all([
    aceitar.evaluate((el) => {
      const s = getComputedStyle(el);
      return { fs: s.fontSize, fw: s.fontWeight, p: s.padding };
    }),
    recusar.evaluate((el) => {
      const s = getComputedStyle(el);
      return { fs: s.fontSize, fw: s.fontWeight, p: s.padding };
    }),
  ]);
  expect(ca).toEqual(cr);

  const [ba, br] = await Promise.all([
    aceitar.boundingBox(),
    recusar.boundingBox(),
  ]);
  expect(Math.abs((ba?.height ?? 0) - (br?.height ?? 0))).toBeLessThan(2);
});

test('a escolha persiste e o banner não volta', async ({ page }) => {
  await page.goto('/');
  await page.getByRole('button', { name: 'Recusar todos' }).click();
  await expect(page.getByRole('dialog', { name: /privacidade/i })).toBeHidden();

  await page.goto('/areas');
  await expect(page.getByRole('dialog', { name: /privacidade/i })).toBeHidden();
});

test('sem consentimento, nenhum script de terceiro é solicitado', async ({ page }) => {
  const terceiros: string[] = [];
  page.on('request', (r) => {
    const u = r.url();
    if (/googletagmanager|google-analytics|clarity\.ms|facebook\.(net|com)/.test(u)) {
      terceiros.push(u);
    }
  });
  await page.goto('/', { waitUntil: 'networkidle' });
  await page.getByRole('button', { name: 'Recusar todos' }).click();
  await page.waitForTimeout(500);
  expect(terceiros).toEqual([]);
});
```

- [ ] **Step 6: Implementar `components/consent-banner.tsx`**

```tsx
'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { readConsent, writeConsent } from '@/lib/consent';

/**
 * Consentimento granular, sem dark pattern: "Recusar todos" e "Aceitar todos"
 * usam o mesmo componente de botão, o mesmo tamanho e o mesmo contraste.
 * Reabrível pelo rodapé.
 */
export function ConsentBanner() {
  const [aberto, setAberto] = useState(false);
  const [analytics, setAnalytics] = useState(true);
  const [marketing, setMarketing] = useState(true);

  useEffect(() => {
    if (readConsent() === null) setAberto(true);
    const reabrir = () => setAberto(true);
    window.addEventListener('dsa-consent-reopen', reabrir);
    return () => window.removeEventListener('dsa-consent-reopen', reabrir);
  }, []);

  function decidir(escolha: { analytics: boolean; marketing: boolean }) {
    writeConsent(escolha);
    setAberto(false);
  }

  if (!aberto) return null;

  return (
    <div
      className="consent-banner"
      role="dialog"
      aria-modal="false"
      aria-label="Preferências de privacidade"
    >
      <div className="consent-banner-inner">
        <div className="consent-banner-copy">
          <h2>Preferências de privacidade</h2>
          <p>
            Usamos cookies necessários para o funcionamento do site. Cookies de
            análise e de marketing só são ativados com a sua autorização. Você
            pode mudar essa escolha a qualquer momento no rodapé. Detalhes na{' '}
            <Link href="/politica-de-privacidade">Política de Privacidade</Link>.
          </p>
          <ul className="consent-options">
            <li>
              <label className="choice">
                <input type="checkbox" checked disabled readOnly />
                <span>
                  <strong>Necessários</strong> — sempre ativos. Sustentam a
                  navegação e a segurança do site.
                </span>
              </label>
            </li>
            <li>
              <label className="choice">
                <input
                  type="checkbox"
                  checked={analytics}
                  onChange={(e) => setAnalytics(e.target.checked)}
                />
                <span>
                  <strong>Análise</strong> — mostram quais páginas ajudam quem
                  procura o escritório.
                </span>
              </label>
            </li>
            <li>
              <label className="choice">
                <input
                  type="checkbox"
                  checked={marketing}
                  onChange={(e) => setMarketing(e.target.checked)}
                />
                <span>
                  <strong>Marketing</strong> — medem o resultado de anúncios.
                </span>
              </label>
            </li>
          </ul>
        </div>
        <div className="consent-actions">
          <button
            type="button"
            className="btn btn-ghost"
            onClick={() => decidir({ analytics: false, marketing: false })}
          >
            Recusar todos
          </button>
          <button
            type="button"
            className="btn btn-ghost"
            onClick={() => decidir({ analytics, marketing })}
          >
            Salvar escolha
          </button>
          <button
            type="button"
            className="btn btn-ghost"
            onClick={() => decidir({ analytics: true, marketing: true })}
          >
            Aceitar todos
          </button>
        </div>
      </div>
    </div>
  );
}
```

Os três botões usam `btn btn-ghost` — mesma classe, mesmo peso. Nenhum é `btn-primary`, porque destacar "Aceitar" é exatamente o dark pattern que o briefing proíbe.

- [ ] **Step 7: Estilizar o banner**

```css
.consent-banner {
  position: fixed; inset: auto 0 0 0; z-index: 120;
  background: var(--surface-raised); border-top: 1px solid var(--border);
  box-shadow: var(--shadow-lg);
}
.consent-banner-inner {
  max-width: var(--container); margin-inline: auto;
  padding: 1.4rem var(--gutter); display: grid; gap: 1.2rem;
}
@media (min-width: 920px) { .consent-banner-inner { grid-template-columns: 1fr auto; align-items: center; } }
.consent-banner h2 { font-size: 1.05rem; margin-bottom: .3rem; }
.consent-banner p { font-size: .92rem; color: var(--text-muted); margin-bottom: .8rem; }
.consent-options { list-style: none; padding: 0; margin: 0; display: grid; gap: .4rem; font-size: .9rem; }
.consent-actions { display: flex; flex-wrap: wrap; gap: .6rem; }
.consent-actions .btn { flex: 1 1 auto; min-height: 44px; }
```

- [ ] **Step 8: Montar o banner e o link de reabertura**

Em `app/(site)/layout.tsx`, importar e renderizar `<ConsentBanner />` logo antes de `<WhatsappFloat />`.

Em `components/site-footer.tsx`, dentro da lista de links legais do rodapé, acrescentar um botão que reabre o banner. Como o rodapé é server component, criar `components/consent-reopen.tsx`:

```tsx
'use client';

export function ConsentReopen() {
  return (
    <button
      type="button"
      className="link-button"
      onClick={() => window.dispatchEvent(new Event('dsa-consent-reopen'))}
    >
      Preferências de privacidade
    </button>
  );
}
```

```css
.link-button {
  background: none; border: 0; padding: 0; cursor: pointer;
  color: inherit; font: inherit; text-decoration: underline;
  text-underline-offset: 3px;
}
```

- [ ] **Step 9: Rodar e verificar que passa**

Run: `npm run test`
Expected: PASS.

Run: `npm run e2e -- consentimento`
Expected: PASS — 4 testes.

- [ ] **Step 10: Commit**

```bash
git add lib/consent.ts components/consent-banner.tsx components/consent-reopen.tsx components/site-footer.tsx "app/(site)/layout.tsx" app/globals.css tests/unit/consent.test.ts tests/e2e/consentimento.spec.ts
git commit -m "feat: consentimento granular de cookies, sem dark pattern"
```

---

### Task 8: Medição

**Files:**
- Create: `lib/analytics.ts`
- Create: `public/vendor/ga4.js`
- Create: `public/vendor/clarity.js`
- Create: `public/vendor/meta-pixel.js`
- Create: `components/analytics.tsx`
- Create: `components/track-link.tsx`
- Create: `components/track-anchor.tsx`
- Create: `components/track-view.tsx`
- Create: `components/read-progress.tsx`
- Create: `tests/unit/analytics.test.ts`
- Modify: `components/meta-pixel.tsx` (deletar — substituído por `components/analytics.tsx`)
- Modify: `app/(site)/layout.tsx`
- Modify: `components/whatsapp-float.tsx`, `components/diagnostico-form.tsx`, `app/(site)/areas/[slug]/page.tsx`, `app/(site)/modelos/[slug]/page.tsx`, `app/(site)/blog/[slug]/page.tsx`

**Interfaces:**
- Consumes: `readConsent`, `CONSENT_EVENT` de `lib/consent.ts` (Tarefa 7)
- Produces:
  - `type EventName = 'view_area_page' | 'cta_click' | 'whatsapp_click' | 'phone_click' | 'email_click' | 'material_download' | 'article_read_75pct' | 'form_start' | 'generate_lead'`
  - `type EventParams = Record<string, string | number | boolean>`
  - `track(name: EventName, params?: EventParams): void`
  - `<Analytics />`, `<TrackLink>` (mesmo contrato de `next/link` mais `event` e `params`)

- [ ] **Step 1: Escrever os testes unitários**

`tests/unit/analytics.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { track } from '@/lib/analytics';
import { writeConsent } from '@/lib/consent';

type Layer = unknown[][];

function camada(): Layer {
  return (window as unknown as { dataLayer?: Layer }).dataLayer ?? [];
}

beforeEach(() => {
  localStorage.clear();
  (window as unknown as { dataLayer?: Layer }).dataLayer = [];
});

describe('track', () => {
  it('não registra nada sem consentimento', () => {
    track('cta_click', { origem: 'home', destino: '/diagnostico' });
    expect(camada()).toHaveLength(0);
  });

  it('não registra nada quando a análise foi recusada', () => {
    writeConsent({ analytics: false, marketing: false });
    track('cta_click', { origem: 'home', destino: '/diagnostico' });
    expect(camada()).toHaveLength(0);
  });

  it('registra o evento quando a análise foi autorizada', () => {
    writeConsent({ analytics: true, marketing: false });
    track('cta_click', { origem: 'home', destino: '/diagnostico' });
    expect(camada()).toHaveLength(1);
    expect(camada()[0]).toEqual([
      'event',
      'cta_click',
      { origem: 'home', destino: '/diagnostico' },
    ]);
  });

  it('aceita evento sem parâmetros', () => {
    writeConsent({ analytics: true, marketing: false });
    track('form_start');
    expect(camada()[0]).toEqual(['event', 'form_start', {}]);
  });

  it('não lança quando o dataLayer ainda não existe', () => {
    writeConsent({ analytics: true, marketing: false });
    delete (window as unknown as { dataLayer?: Layer }).dataLayer;
    expect(() => track('form_start')).not.toThrow();
  });
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run test -- analytics`
Expected: FAIL — `Cannot find module '@/lib/analytics'`.

- [ ] **Step 3: Implementar `lib/analytics.ts`**

```ts
import { readConsent } from './consent';

export type EventName =
  | 'view_area_page'
  | 'cta_click'
  | 'whatsapp_click'
  | 'phone_click'
  | 'email_click'
  | 'material_download'
  | 'article_read_75pct'
  | 'form_start'
  | 'generate_lead';

export type EventParams = Record<string, string | number | boolean>;

type Layer = { push: (...args: unknown[]) => void };

/**
 * Registra um evento no GA4. Fora de consentimento é um no-op silencioso —
 * a chamada pode ficar espalhada pelo código sem condicional em cada ponto.
 */
export function track(name: EventName, params: EventParams = {}): void {
  if (typeof window === 'undefined') return;
  const consent = readConsent();
  if (!consent?.analytics) return;

  const layer = (window as unknown as { dataLayer?: Layer }).dataLayer;
  if (!layer) return;
  layer.push('event', name, params);
}
```

O `push('event', name, params)` reproduz a assinatura do `gtag('event', ...)`, que empurra o objeto `arguments` para o `dataLayer`. Ver `public/vendor/ga4.js` no Step 5.

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm run test -- analytics`
Expected: PASS — 5 testes.

- [ ] **Step 5: Criar os inits de mesma origem**

Nenhum destes tem corpo inline no HTML — são arquivos servidos de `'self'`, configurados por `data-*` lidos com `document.currentScript`. É isso que permite o `script-src` sem `unsafe-inline` da Tarefa 9.

`public/vendor/ga4.js`:

```js
/* GA4. Configurado por data-ga-id. Só é anexado ao DOM depois do
   consentimento de análise — ver components/analytics.tsx. */
(function () {
  var el = document.currentScript;
  var id = el && el.dataset ? el.dataset.gaId : null;
  if (!id) return;

  window.dataLayer = window.dataLayer || [];
  function gtag() { window.dataLayer.push(arguments); }
  window.gtag = gtag;

  gtag('consent', 'default', {
    ad_storage: 'denied',
    ad_user_data: 'denied',
    ad_personalization: 'denied',
    analytics_storage: 'denied'
  });
  gtag('consent', 'update', { analytics_storage: 'granted' });
  if (el.dataset.marketing === '1') {
    gtag('consent', 'update', {
      ad_storage: 'granted',
      ad_user_data: 'granted',
      ad_personalization: 'granted'
    });
  }

  gtag('js', new Date());
  gtag('config', id, { send_page_view: true });

  var s = document.createElement('script');
  s.async = true;
  s.src = 'https://www.googletagmanager.com/gtag/js?id=' + encodeURIComponent(id);
  document.head.appendChild(s);
})();
```

`public/vendor/clarity.js`:

```js
/* Microsoft Clarity (heatmap). Configurado por data-clarity-id. */
(function () {
  var el = document.currentScript;
  var id = el && el.dataset ? el.dataset.clarityId : null;
  if (!id) return;

  window.clarity = window.clarity || function () {
    (window.clarity.q = window.clarity.q || []).push(arguments);
  };
  var s = document.createElement('script');
  s.async = true;
  s.src = 'https://www.clarity.ms/tag/' + encodeURIComponent(id);
  document.head.appendChild(s);
})();
```

`public/vendor/meta-pixel.js`:

```js
/* Pixel da Meta. Configurado por data-pixel-id. Só carrega sob
   consentimento de marketing. */
(function () {
  var el = document.currentScript;
  var id = el && el.dataset ? el.dataset.pixelId : null;
  if (!id) return;

  if (!window.fbq) {
    var n = function () {
      n.callMethod ? n.callMethod.apply(n, arguments) : n.queue.push(arguments);
    };
    n.push = n; n.loaded = true; n.version = '2.0'; n.queue = [];
    window.fbq = n;
    window._fbq = n;
  }
  var s = document.createElement('script');
  s.async = true;
  s.src = 'https://connect.facebook.net/en_US/fbevents.js';
  document.head.appendChild(s);

  window.fbq('init', id);
  window.fbq('track', 'PageView');
})();
```

- [ ] **Step 6: Criar `components/analytics.tsx`**

```tsx
'use client';

import { useEffect } from 'react';
import { CONSENT_EVENT, readConsent } from '@/lib/consent';

function anexar(src: string, dataset: Record<string, string>) {
  if (document.querySelector(`script[src="${src}"]`)) return;
  const s = document.createElement('script');
  s.src = src;
  s.async = true;
  for (const [k, v] of Object.entries(dataset)) s.dataset[k] = v;
  document.head.appendChild(s);
}

/**
 * Carrega os provedores de medição — e só depois do consentimento.
 * Cada um é env-gated: sem a variável, nada é anexado e o site funciona
 * exatamente como antes de a conta existir.
 */
export function Analytics() {
  useEffect(() => {
    const gaId = process.env.NEXT_PUBLIC_GA_ID;
    const clarityId = process.env.NEXT_PUBLIC_CLARITY_ID;
    const pixelId = process.env.NEXT_PUBLIC_META_PIXEL_ID;

    function aplicar() {
      const c = readConsent();
      if (!c) return;
      if (c.analytics && gaId) {
        anexar('/vendor/ga4.js', {
          gaId,
          marketing: c.marketing ? '1' : '0',
        });
      }
      if (c.analytics && clarityId) {
        anexar('/vendor/clarity.js', { clarityId });
      }
      if (c.marketing && pixelId) {
        anexar('/vendor/meta-pixel.js', { pixelId });
      }
    }

    aplicar();
    window.addEventListener(CONSENT_EVENT, aplicar);
    return () => window.removeEventListener(CONSENT_EVENT, aplicar);
  }, []);

  return null;
}
```

Deletar `components/meta-pixel.tsx` — o script inline dele é incompatível com o CSP da Tarefa 9. Conferir com `grep -rn "meta-pixel\|pixelEvento" app components` que nada mais o importa; se `pixelEvento` estiver em uso, substituir a chamada por `track('generate_lead', ...)`.

- [ ] **Step 7: Criar `components/track-link.tsx`**

```tsx
'use client';

import Link from 'next/link';
import type { ComponentProps } from 'react';
import { track, type EventName, type EventParams } from '@/lib/analytics';

type Props = ComponentProps<typeof Link> & {
  event: EventName;
  params?: EventParams;
};

/** Link que registra um evento no clique. A navegação nunca depende do registro. */
export function TrackLink({ event, params, onClick, ...rest }: Props) {
  return (
    <Link
      {...rest}
      onClick={(e) => {
        track(event, params);
        onClick?.(e);
      }}
    />
  );
}
```

- [ ] **Step 8: Instrumentar os pontos existentes**

Criar `components/track-view.tsx` — dispara um evento de visualização uma única vez:

```tsx
'use client';

import { useEffect, useRef } from 'react';
import { track, type EventName, type EventParams } from '@/lib/analytics';

/** Registra um evento de visualização uma vez por montagem. Não renderiza nada. */
export function TrackView({
  event,
  params,
}: {
  event: EventName;
  params?: EventParams;
}) {
  const chave = JSON.stringify(params ?? {});
  const jaEnviou = useRef('');

  useEffect(() => {
    if (jaEnviou.current === chave) return;
    jaEnviou.current = chave;
    track(event, params);
    // `chave` já representa `params`; incluir o objeto causaria reenvio a cada render.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [event, chave]);

  return null;
}
```

Criar `components/read-progress.tsx` — o evento de leitura de 75%:

```tsx
'use client';

import { useEffect } from 'react';
import { track } from '@/lib/analytics';

/**
 * Dispara `article_read_75pct` uma única vez, quando o leitor passa de 75%
 * da altura do artigo. Usa rolagem passiva — não bloqueia a thread.
 */
export function ReadProgress({ slug }: { slug: string }) {
  useEffect(() => {
    const artigo = document.querySelector('.prose');
    if (!artigo) return;

    let disparado = false;
    const aoRolar = () => {
      if (disparado) return;
      const r = artigo.getBoundingClientRect();
      const lido = (window.innerHeight - r.top) / r.height;
      if (lido >= 0.75) {
        disparado = true;
        track('article_read_75pct', { slug });
        window.removeEventListener('scroll', aoRolar);
      }
    };

    window.addEventListener('scroll', aoRolar, { passive: true });
    aoRolar();
    return () => window.removeEventListener('scroll', aoRolar);
  }, [slug]);

  return null;
}
```

Criar `components/track-anchor.tsx` — para `tel:`, `mailto:` e `wa.me`, que não passam pelo `next/link`:

```tsx
'use client';

import type { AnchorHTMLAttributes } from 'react';
import { track, type EventName, type EventParams } from '@/lib/analytics';

type Props = AnchorHTMLAttributes<HTMLAnchorElement> & {
  event: EventName;
  params?: EventParams;
};

/** Âncora comum que registra um evento no clique. A navegação nunca depende do registro. */
export function TrackAnchor({ event, params, onClick, ...rest }: Props) {
  return (
    <a
      {...rest}
      onClick={(e) => {
        track(event, params);
        onClick?.(e);
      }}
    />
  );
}
```

Converter `components/whatsapp-float.tsx` para usar `TrackAnchor` — e, de passagem, montar a mensagem contextual (item 4 do §10 do briefing), que exige saber a rota:

```tsx
'use client';

import { usePathname } from 'next/navigation';
import { TrackAnchor } from './track-anchor';

/** Rótulo humano da página atual, para a mensagem pré-preenchida. */
function contextoDaRota(pathname: string): string {
  if (pathname.startsWith('/areas/')) {
    const slug = pathname.split('/')[2] ?? '';
    return `a página de ${slug.replace(/-/g, ' ')}`;
  }
  if (pathname.startsWith('/blog/')) return 'um artigo do blog';
  if (pathname.startsWith('/modelos')) return 'a página de materiais';
  if (pathname.startsWith('/diagnostico')) return 'a página de diagnóstico';
  if (pathname.startsWith('/areas')) return 'a página de áreas de atuação';
  if (pathname.startsWith('/contato')) return 'a página de contato';
  return 'o site';
}

export function WhatsappFloat({ whatsapp }: { whatsapp: string }) {
  const pathname = usePathname();
  const contexto = contextoDaRota(pathname);
  const text = encodeURIComponent(
    `Olá, vim de ${contexto} e gostaria de agendar um atendimento.`
  );

  return (
    <TrackAnchor
      className="wa-float"
      href={`https://wa.me/${whatsapp}?text=${text}`}
      target="_blank"
      rel="noopener"
      aria-label="Falar no WhatsApp"
      event="whatsapp_click"
      params={{ origem: pathname }}
    >
      {/* Manter o <svg> existente, sem alteração. */}
    </TrackAnchor>
  );
}
```

Montar o restante:

- `app/(site)/layout.tsx`: renderizar `<Analytics />` junto de `<ConsentBanner />`.
- `components/site-footer.tsx` e `app/(site)/contato/page.tsx`: trocar os `<a href="tel:...">` e `<a href="mailto:...">` por `TrackAnchor` com `event="phone_click"` / `event="email_click"` e `params={{ origem: 'rodape' }}` / `{{ origem: 'contato' }}`.
- `app/(site)/areas/[slug]/page.tsx`: `<TrackView event="view_area_page" params={{ area: slug }} />`.
- `app/(site)/modelos/[slug]/page.tsx`: no link de download, `TrackAnchor` com `event="material_download"` e `params={{ slug }}`.
- `app/(site)/blog/[slug]/page.tsx`: `<ReadProgress slug={slug} />`.
- CTAs principais da home, `/areas` e `/areas/[slug]`: trocar `Link` por `TrackLink` com `event="cta_click"` e `params={{ origem, destino }}`.

Em `components/diagnostico-form.tsx`, acrescentar uma ref e dois disparos — sem alterar a lógica de validação nem a copy:

```tsx
const iniciado = useRef(false);

// dentro do onInput já existente do <form>, antes de clearError:
if (!iniciado.current) {
  iniciado.current = true;
  track('form_start');
}

// dentro do try do onSubmit, logo após setStatus('sent'):
track('generate_lead', {
  frente: String(data.get('frente') ?? ''),
  tem_prazo: String(data.get('prazo') ?? ''),
});
```

- [ ] **Step 9: Verificar**

Run: `npm run test`
Expected: PASS.

Run: `npm run e2e`
Expected: PASS — em especial o teste "sem consentimento, nenhum script de terceiro é solicitado" da Tarefa 7, que agora exercita código real.

Verificação manual com `NEXT_PUBLIC_GA_ID` definido: aceitar o consentimento, abrir o DevTools e confirmar que `/vendor/ga4.js` é carregado, que `window.dataLayer` recebe os eventos e que nenhum `<script>` inline aparece no HTML (`view-source` + busca por `<script>` sem `src`).

- [ ] **Step 10: Commit**

```bash
git rm components/meta-pixel.tsx
git add lib/analytics.ts public/vendor components/analytics.tsx components/track-link.tsx components/track-view.tsx components/read-progress.tsx components/whatsapp-float.tsx components/site-footer.tsx components/diagnostico-form.tsx "app/(site)" tests/unit/analytics.test.ts
git commit -m "feat: medição GA4/Clarity/Pixel atrás de consentimento, sem script inline"
```

---

### Task 9: Headers de segurança e cache

Só é possível agora: as Tarefas 3 e 8 eliminaram o último script inline.

**Files:**
- Create: `lib/csp.ts`
- Create: `tests/unit/csp.test.ts`
- Create: `tests/e2e/seguranca.spec.ts`
- Modify: `next.config.mjs`

**Interfaces:**
- Consumes: os hosts de terceiro definidos em `public/vendor/*.js` (Tarefa 8)
- Produces: `buildCsp(): string`, `getDirective(policy: string, name: string): string[]`, `SECURITY_HEADERS: { key: string; value: string }[]`

- [ ] **Step 1: Escrever os testes unitários**

`tests/unit/csp.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { buildCsp, getDirective, SECURITY_HEADERS } from '@/lib/csp';

const policy = buildCsp();

describe('Content-Security-Policy', () => {
  it('script-src permite só a própria origem e os hosts de medição', () => {
    const src = getDirective(policy, 'script-src');
    expect(src).toContain("'self'");
    expect(src).toContain('https://www.googletagmanager.com');
    expect(src).toContain('https://connect.facebook.net');
    expect(src).toContain('https://www.clarity.ms');
  });

  it('script-src NÃO permite unsafe-inline nem unsafe-eval', () => {
    const src = getDirective(policy, 'script-src');
    expect(src).not.toContain("'unsafe-inline'");
    expect(src).not.toContain("'unsafe-eval'");
  });

  it('style-src permite unsafe-inline — decisão registrada no spec §5.2', () => {
    expect(getDirective(policy, 'style-src')).toContain("'unsafe-inline'");
  });

  it('bloqueia enquadramento, objetos e mudança de base', () => {
    expect(getDirective(policy, 'frame-ancestors')).toEqual(["'none'"]);
    expect(getDirective(policy, 'object-src')).toEqual(["'none'"]);
    expect(getDirective(policy, 'base-uri')).toEqual(["'self'"]);
    expect(getDirective(policy, 'form-action')).toEqual(["'self'"]);
  });

  it('libera o iframe do Google Maps usado em /contato', () => {
    expect(getDirective(policy, 'frame-src')).toContain('https://www.google.com');
  });

  it('libera o webhook usado hoje pelo formulário de diagnóstico', () => {
    expect(getDirective(policy, 'connect-src')).toContain(
      'https://webhook.licitacaogc.com.br'
    );
  });

  it('não tem diretiva vazia', () => {
    for (const parte of policy.split(';')) {
      if (parte.trim() === '') continue;
      expect(parte.trim().split(/\s+/).length).toBeGreaterThanOrEqual(1);
    }
  });
});

describe('demais headers', () => {
  const mapa = new Map(SECURITY_HEADERS.map((h) => [h.key, h.value]));

  it('traz os sete headers exigidos', () => {
    for (const k of [
      'Content-Security-Policy',
      'Strict-Transport-Security',
      'X-Content-Type-Options',
      'X-Frame-Options',
      'Referrer-Policy',
      'Permissions-Policy',
      'Cross-Origin-Opener-Policy',
    ]) {
      expect(mapa.has(k), `falta ${k}`).toBe(true);
    }
  });

  it('HSTS tem dois anos, subdomínios e preload', () => {
    expect(mapa.get('Strict-Transport-Security')).toBe(
      'max-age=63072000; includeSubDomains; preload'
    );
  });

  it('Permissions-Policy desliga câmera, microfone e geolocalização', () => {
    const v = mapa.get('Permissions-Policy') ?? '';
    expect(v).toContain('camera=()');
    expect(v).toContain('microphone=()');
    expect(v).toContain('geolocation=()');
  });
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run test -- csp`
Expected: FAIL — `Cannot find module '@/lib/csp'`.

- [ ] **Step 3: Implementar `lib/csp.ts`**

```ts
/**
 * CSP sem nonce e sem unsafe-inline em script-src.
 *
 * Isso só é possível porque o site não tem NENHUM <script> inline: o
 * bootstrap de tema e reveal vive em /bootstrap.js e os inits de medição
 * em /vendor/*.js, todos de mesma origem. A alternativa canônica do Next
 * — nonce via middleware — forçaria renderização dinâmica em todas as
 * páginas e destruiria o TTFB.
 *
 * JSON-LD permanece inline: `type="application/ld+json"` não é executável
 * e não é governado por script-src.
 */
const DIRECTIVES: Record<string, string[]> = {
  'default-src': ["'self'"],
  'script-src': [
    "'self'",
    'https://www.googletagmanager.com',
    'https://connect.facebook.net',
    'https://www.clarity.ms',
    'https://va.vercel-scripts.com',
  ],
  // Decisão registrada no spec §5.2: o Next injeta estilos inline para
  // next/font e para o CSS crítico do App Router. Risco muito menor que
  // o equivalente em script-src, e documentado como escolha.
  'style-src': ["'self'", "'unsafe-inline'"],
  'img-src': [
    "'self'",
    'data:',
    'blob:',
    'https://www.google-analytics.com',
    'https://www.facebook.com',
    'https://*.clarity.ms',
    'https://maps.gstatic.com',
    'https://*.googleapis.com',
  ],
  'font-src': ["'self'", 'data:'],
  'media-src': ["'self'"],
  'connect-src': [
    "'self'",
    'https://*.google-analytics.com',
    'https://*.analytics.google.com',
    'https://*.googletagmanager.com',
    'https://*.clarity.ms',
    // Endpoint atual do formulário de diagnóstico. Sai quando o Sprint 2
    // mover o envio para uma Server Action.
    'https://webhook.licitacaogc.com.br',
    'https://va.vercel-scripts.com',
  ],
  'frame-src': ['https://www.google.com', 'https://maps.google.com'],
  'object-src': ["'none'"],
  'base-uri': ["'self'"],
  'form-action': ["'self'"],
  'frame-ancestors': ["'none'"],
};

export function buildCsp(): string {
  const partes = Object.entries(DIRECTIVES).map(
    ([nome, valores]) => `${nome} ${valores.join(' ')}`
  );
  partes.push('upgrade-insecure-requests');
  return partes.join('; ');
}

/** Extrai os valores de uma diretiva. Usado nos testes. */
export function getDirective(policy: string, name: string): string[] {
  const parte = policy
    .split(';')
    .map((p) => p.trim())
    .find((p) => p === name || p.startsWith(name + ' '));
  if (!parte) return [];
  return parte.split(/\s+/).slice(1);
}

export const SECURITY_HEADERS: { key: string; value: string }[] = [
  { key: 'Content-Security-Policy', value: buildCsp() },
  {
    key: 'Strict-Transport-Security',
    value: 'max-age=63072000; includeSubDomains; preload',
  },
  { key: 'X-Content-Type-Options', value: 'nosniff' },
  { key: 'X-Frame-Options', value: 'DENY' },
  { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
  {
    key: 'Permissions-Policy',
    value: 'camera=(), microphone=(), geolocation=()',
  },
  { key: 'Cross-Origin-Opener-Policy', value: 'same-origin' },
];
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `npm run test -- csp`
Expected: PASS — 10 testes.

- [ ] **Step 5: Escrever o teste E2E**

`tests/e2e/seguranca.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

const EXIGIDOS = [
  'content-security-policy',
  'strict-transport-security',
  'x-content-type-options',
  'x-frame-options',
  'referrer-policy',
  'permissions-policy',
  'cross-origin-opener-policy',
];

test('as páginas do site trazem os sete headers', async ({ request }) => {
  for (const rota of ['/', '/areas', '/blog', '/contato', '/diagnostico']) {
    const res = await request.get(rota);
    const h = res.headers();
    for (const nome of EXIGIDOS) {
      expect(h[nome], `${rota} não tem ${nome}`).toBeTruthy();
    }
    expect(h['content-security-policy']).not.toContain("script-src 'self' 'unsafe-inline'");
  }
});

test('o HTML não tem nenhum script inline executável', async ({ page }) => {
  await page.goto('/');
  const inlineExecutaveis = await page.evaluate(() =>
    Array.from(document.querySelectorAll('script'))
      .filter((s) => !s.src)
      .filter((s) => {
        const t = (s.getAttribute('type') || '').toLowerCase();
        return t !== 'application/ld+json';
      })
      .map((s) => s.textContent?.slice(0, 80) ?? '')
  );
  expect(inlineExecutaveis).toEqual([]);
});

test('nenhuma violação de CSP é registrada na navegação principal', async ({ page }) => {
  const violacoes: string[] = [];
  page.on('console', (msg) => {
    if (/Content Security Policy/i.test(msg.text())) violacoes.push(msg.text());
  });
  for (const rota of ['/', '/areas', '/sobre', '/blog', '/contato']) {
    await page.goto(rota, { waitUntil: 'networkidle' });
  }
  expect(violacoes).toEqual([]);
});
```

- [ ] **Step 6: Aplicar os headers em `next.config.mjs`**

```js
import { SECURITY_HEADERS } from './lib/csp.ts';

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  async headers() {
    return [
      {
        // Tudo, menos o CMS e a API dele: o Keystatic usa recursos que
        // o CSP estrito do site público quebraria.
        source: '/((?!keystatic|api/keystatic).*)',
        headers: [
          ...SECURITY_HEADERS,
          // Substitui o `max-age=0, must-revalidate` atual: o HTML passa a
          // ser cacheável na CDN, o que só é possível porque as páginas
          // continuam estáticas (sem nonce por requisição).
          {
            key: 'Cache-Control',
            value: 'public, s-maxage=3600, stale-while-revalidate=86400',
          },
        ],
      },
    ];
  },
};

export default nextConfig;
```

Se o Next não conseguir importar `./lib/csp.ts` a partir de um `.mjs`, converter `next.config.mjs` para `next.config.ts` (suportado no Next 15+) e ajustar `vercel.json` se necessário. Verificar com `npm run build` antes de seguir.

- [ ] **Step 7: Rodar e verificar que passa**

Run: `npm run e2e -- seguranca`
Expected: PASS — 3 testes.

Run: `npm run e2e`
Expected: PASS. Atenção especial ao teste de tema e ao de reveal: se o CSP bloquear `/bootstrap.js`, eles falham primeiro — é a rede de proteção funcionando.

Verificar manualmente que `/keystatic` ainda abre e funciona.

- [ ] **Step 8: Commit**

```bash
git add lib/csp.ts next.config.mjs tests/unit/csp.test.ts tests/e2e/seguranca.spec.ts
git commit -m "feat: sete headers de segurança e CSP sem unsafe-inline (P7)"
```

---

### Task 10: P8 — grid de cards e ícones Lucide

**Files:**
- Modify: `app/globals.css` (bloco `.card` / `.area-card`)
- Modify: `components/area-icon.tsx`
- Create: `tests/e2e/cards.spec.ts`

**Interfaces:**
- Consumes: `lucide-react` (instalado na Tarefa 3)
- Produces: `<AreaIcon icon={string} />` mantém a mesma assinatura — só a implementação muda

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/cards.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('os links "Ver área" alinham na mesma linha do grid', async ({ page }) => {
  await page.setViewportSize({ width: 1280, height: 900 });
  await page.goto('/areas');

  const tops = await page.locator('a.area-card .card-link').evaluateAll((els) =>
    els.map((el) => Math.round(el.getBoundingClientRect().top))
  );

  // Agrupa por linha do grid: cards da mesma linha têm o mesmo topo de card.
  const cardTops = await page.locator('a.area-card').evaluateAll((els) =>
    els.map((el) => Math.round(el.getBoundingClientRect().top))
  );

  const porLinha = new Map<number, number[]>();
  cardTops.forEach((t, i) => {
    const chave = [...porLinha.keys()].find((k) => Math.abs(k - t) < 8) ?? t;
    porLinha.set(chave, [...(porLinha.get(chave) ?? []), tops[i]]);
  });

  for (const [linha, linksDaLinha] of porLinha) {
    const min = Math.min(...linksDaLinha);
    const max = Math.max(...linksDaLinha);
    expect(max - min, `linha ${linha} desalinhada em ${max - min}px`).toBeLessThanOrEqual(2);
  }
});

test('os ícones de área têm tamanho uniforme', async ({ page }) => {
  await page.goto('/areas');
  const tamanhos = await page.locator('a.area-card .card-icon').evaluateAll((els) =>
    els.map((el) => {
      const r = el.getBoundingClientRect();
      return `${Math.round(r.width)}x${Math.round(r.height)}`;
    })
  );
  expect(new Set(tamanhos).size).toBe(1);
  expect(tamanhos[0]).toBe('48x48');
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- cards`
Expected: FAIL — os links desalinham (títulos de 1, 2 e 3 linhas) e os ícones têm 52px.

- [ ] **Step 3: Corrigir o CSS**

Substituir os blocos `.card` e `.area-card` em `app/globals.css`:

```css
.card {
  background: var(--surface-raised); border: 1px solid var(--border);
  border-radius: var(--radius-lg); padding: 1.6rem; height: 100%;
  transition: transform .16s var(--ease), border-color .16s var(--ease), background .16s var(--ease);
}

.area-card {
  display: grid;
  /* ícone · título · descrição (cresce) · link */
  grid-template-rows: auto auto 1fr auto;
  text-decoration: none; color: var(--text);
}
/* Reserva duas linhas para o título: "Mandado de Segurança" e
   "Defesa de Agentes Públicos" passam a ocupar a mesma altura. */
.area-card h3 { margin-bottom: .35rem; min-height: 2lh; }
.area-card p { color: var(--text-muted); font-size: .96rem; margin-bottom: 1rem; }
.area-card .card-icon {
  color: var(--accent); margin-bottom: 1rem;
  width: 48px; height: 48px; flex: none; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  background: var(--accent-soft);
}
.area-card .card-link {
  font-weight: 600; font-size: .9rem; color: var(--accent);
  display: inline-flex; align-items: center; gap: .35rem;
}
/* A borda é o separador padrão do sistema; a sombra fica reservada
   a elementos flutuantes. */
.area-card:hover {
  transform: translateY(-2px);
  border-color: var(--accent);
  background: color-mix(in srgb, var(--accent) 2%, var(--surface-raised));
  color: var(--text);
}
.area-card:hover .card-link { gap: .6rem; }
```

Aplicar o mesmo tratamento a `.post-card` (troca de `box-shadow: var(--shadow-lg)` por borda + fundo).

`min-height: 2lh` depende de suporte a `lh`. Acrescentar um fallback:

```css
.area-card h3 { min-height: 2.6em; min-height: 2lh; }
```

- [ ] **Step 4: Migrar `components/area-icon.tsx` para Lucide**

Manter a assinatura e o mapa de chaves do `keystatic.config.ts` (`shield`, `doc`, `contract`, `check-doc`, `person`, `shield-check`, `database`, `ledger`, `users`, `shield-heart`, `briefcase`, `doc-shield`, `coins`, `percent`).

```tsx
import {
  Shield, FileText, FileSignature, FileCheck2, User, ShieldCheck,
  Database, BookOpenCheck, Users, HeartHandshake, Briefcase,
  FileWarning, Coins, Percent,
} from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

const MAPA: Record<string, LucideIcon> = {
  'shield': Shield,
  'doc': FileText,
  'contract': FileSignature,
  'check-doc': FileCheck2,
  'person': User,
  'shield-check': ShieldCheck,
  'database': Database,
  'ledger': BookOpenCheck,
  'users': Users,
  'shield-heart': HeartHandshake,
  'briefcase': Briefcase,
  'doc-shield': FileWarning,
  'coins': Coins,
  'percent': Percent,
};

/** Ícone da área. Decorativo: o nome da área já está no <h3> ao lado. */
export function AreaIcon({ icon }: { icon: string }) {
  const Icone = MAPA[icon] ?? Shield;
  return <Icone size={24} strokeWidth={1.5} aria-hidden="true" />;
}
```

Conferir que todos os 14 valores de `ICON_OPTIONS` do `keystatic.config.ts` têm entrada no mapa.

- [ ] **Step 5: Rodar e verificar que passa**

Run: `npm run e2e -- cards`
Expected: PASS — 2 testes.

Run: `npm run e2e`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add app/globals.css components/area-icon.tsx tests/e2e/cards.spec.ts
git commit -m "fix: alinhamento vertical dos cards e ícones Lucide uniformes (P8)"
```

---

### Task 11: P9 — colunas vazias nos heroes internos

**Files:**
- Create: `components/hero-aside.tsx`
- Modify: `app/globals.css`
- Modify: `app/(site)/areas/page.tsx`, `app/(site)/diagnostico/page.tsx`, `app/(site)/contato/page.tsx`, `app/(site)/modelos/page.tsx`
- Create: `tests/e2e/layout.spec.ts`

**Interfaces:**
- Consumes: `getSettings()` de `lib/reader.ts`; `track` de `lib/analytics.ts`
- Produces: `<HeroAside whatsapp={string} contexto={string} titulo?={string} />` — server component; o link de WhatsApp já sai com mensagem contextual pré-preenchida

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/layout.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

const ROTAS = ['/areas', '/diagnostico', '/contato', '/modelos'];

test('nenhum hero interno deixa metade da largura vazia em desktop', async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 900 });
  for (const rota of ROTAS) {
    await page.goto(rota);
    const hero = page.locator('.page-hero .container').first();
    const caixa = await hero.boundingBox();
    const conteudo = await hero.evaluate((el) => {
      let direita = 0;
      for (const filho of el.querySelectorAll('h1, p, nav, .hero-aside')) {
        const r = filho.getBoundingClientRect();
        direita = Math.max(direita, r.right);
      }
      return direita;
    });
    const usado = (conteudo - (caixa?.x ?? 0)) / (caixa?.width ?? 1);
    expect(usado, `${rota} usa só ${(usado * 100).toFixed(0)}% da largura`).toBeGreaterThan(0.6);
  }
});

test('o WhatsApp do cartão de contexto leva mensagem da página', async ({ page }) => {
  await page.goto('/areas');
  const href = await page.locator('.hero-aside a[href*="wa.me"]').getAttribute('href');
  expect(href).toContain('text=');
  expect(decodeURIComponent(href ?? '')).toMatch(/áreas de atuação/i);
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- layout`
Expected: FAIL — o conteúdo ocupa ~50% e `.hero-aside` não existe.

- [ ] **Step 3: Criar `components/hero-aside.tsx`**

```tsx
import Link from 'next/link';
import { MessageCircle, Clock } from 'lucide-react';

/**
 * Preenche a coluna direita dos heroes internos com o que o visitante
 * com prazo em curso precisa: um caminho direto. A mensagem do WhatsApp
 * já sai preenchida com o contexto da página.
 */
export function HeroAside({
  whatsapp,
  contexto,
  titulo = 'Prazo em curso?',
}: {
  whatsapp: string;
  contexto: string;
  titulo?: string;
}) {
  const texto = encodeURIComponent(
    `Olá, vim da página de ${contexto} do site e gostaria de falar sobre o meu caso.`
  );

  return (
    <aside className="hero-aside" aria-label="Contato rápido">
      <p className="hero-aside-eyebrow">
        <Clock size={16} strokeWidth={1.5} aria-hidden="true" />
        {titulo}
      </p>
      <p>
        Quando há prazo correndo, o tempo de reação pesa tanto quanto a tese.
        Fale agora ou envie o caso para triagem técnica.
      </p>
      <div className="hero-aside-actions">
        <a
          className="btn btn-primary btn-block"
          href={`https://wa.me/${whatsapp}?text=${texto}`}
          target="_blank"
          rel="noopener"
        >
          <MessageCircle size={18} strokeWidth={1.5} aria-hidden="true" />
          Falar no WhatsApp
        </a>
        <Link className="btn btn-ghost btn-block" href="/diagnostico">
          Enviar para triagem
        </Link>
      </div>
      <p className="hero-aside-note">
        O contato inicial não constitui mandato profissional nem gera honorários.
      </p>
    </aside>
  );
}
```

- [ ] **Step 4: Estilizar**

```css
.page-hero .container { display: grid; gap: clamp(1.6rem, 4vw, 3rem); }
@media (min-width: 980px) {
  .page-hero .container { grid-template-columns: 1.35fr .65fr; align-items: center; }
  .page-hero .breadcrumb { grid-column: 1 / -1; }
}
/* Sem cartão de contexto, o hero colapsa em coluna única legível
   em vez de deixar metade da largura vazia. */
.page-hero .container:not(:has(.hero-aside)) { display: block; }
.page-hero .container:not(:has(.hero-aside)) .lead { max-width: 68ch; }

.hero-aside {
  background: color-mix(in srgb, var(--paper) 6%, transparent);
  border: 1px solid var(--border-inverse);
  border-radius: var(--radius-lg); padding: 1.5rem;
}
.hero-aside p { color: var(--text-on-inverse); font-size: .94rem; }
.hero-aside-eyebrow {
  display: flex; align-items: center; gap: .45rem;
  font-size: .78rem; font-weight: 700; letter-spacing: .1em;
  text-transform: uppercase; color: var(--brand-100); margin-bottom: .7rem;
}
.hero-aside-actions { display: grid; gap: .6rem; margin: 1.1rem 0 .9rem; }
.hero-aside-note { font-size: .78rem; color: var(--ink-300); margin: 0; }
```

- [ ] **Step 5: Montar nas quatro páginas**

Em cada uma, importar `getSettings` e `HeroAside`, e inserir o componente como último filho do `.container` do `.page-hero`:

- `/areas` → `contexto="áreas de atuação"`
- `/diagnostico` → `contexto="diagnóstico jurídico inicial"`
- `/contato` → `contexto="contato"`
- `/modelos` → `contexto="materiais gratuitos"`

`app/(site)/modelos/page.tsx` já é async? Se não for, torná-la async para chamar `getSettings()`.

- [ ] **Step 6: Rodar e verificar que passa**

Run: `npm run e2e -- layout`
Expected: PASS — 2 testes.

- [ ] **Step 7: Commit**

```bash
git add components/hero-aside.tsx app/globals.css "app/(site)/areas/page.tsx" "app/(site)/diagnostico/page.tsx" "app/(site)/contato/page.tsx" "app/(site)/modelos/page.tsx" tests/e2e/layout.spec.ts
git commit -m "feat: cartão de contexto preenche a coluna vazia dos heroes internos (P9)"
```

---

### Task 12: P11 — mapa sob interação

**Files:**
- Create: `components/map-embed.tsx`
- Modify: `app/(site)/contato/page.tsx:115-130`
- Modify: `app/globals.css`
- Create: `tests/e2e/mapa.spec.ts`

**Interfaces:**
- Consumes: nada
- Produces: `<MapEmbed query={string} titulo={string} />`

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/mapa.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('o mapa não carrega antes do clique e não causa deslocamento', async ({ page }) => {
  await page.goto('/contato');

  await expect(page.locator('.map-embed iframe')).toHaveCount(0);

  const antes = await page.locator('.map-embed').boundingBox();
  await page.getByRole('button', { name: /ver no mapa/i }).click();
  await expect(page.locator('.map-embed iframe')).toHaveCount(1);
  const depois = await page.locator('.map-embed').boundingBox();

  expect(Math.abs((antes?.height ?? 0) - (depois?.height ?? 0))).toBeLessThan(2);
});

test('sem JavaScript, há um link funcional para o Google Maps', async ({ browser }) => {
  const ctx = await browser.newContext({ javaScriptEnabled: false });
  const page = await ctx.newPage();
  await page.goto('/contato');
  const link = page.locator('.map-embed a[href*="google.com/maps"]');
  await expect(link).toBeVisible();
  await ctx.close();
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- mapa`
Expected: FAIL — o iframe já está no DOM desde o carregamento.

- [ ] **Step 3: Criar `components/map-embed.tsx`**

```tsx
'use client';

import { useState } from 'react';
import { MapPin } from 'lucide-react';

/**
 * O iframe do Google só entra no DOM sob clique. O esqueleto tem proporção
 * fixa, então a troca não desloca nada (CLS zero), e o link direto para o
 * Google Maps funciona mesmo sem JavaScript.
 */
export function MapEmbed({ query, titulo }: { query: string; titulo: string }) {
  const [carregar, setCarregar] = useState(false);
  const q = encodeURIComponent(query);

  return (
    <div className="map-embed">
      {carregar ? (
        <iframe
          src={`https://www.google.com/maps?q=${q}&output=embed`}
          title={titulo}
          loading="lazy"
          referrerPolicy="no-referrer-when-downgrade"
        />
      ) : (
        <div className="map-placeholder">
          <MapPin size={28} strokeWidth={1.5} aria-hidden="true" />
          <p>{query}</p>
          <button
            type="button"
            className="btn btn-ghost"
            onClick={() => setCarregar(true)}
          >
            Ver no mapa
          </button>
          <a
            href={`https://www.google.com/maps/search/?api=1&query=${q}`}
            target="_blank"
            rel="noopener"
          >
            Abrir no Google Maps
          </a>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Estilizar com proporção fixa**

```css
.map-embed {
  position: relative; aspect-ratio: 16 / 9;
  border: 1px solid var(--border); border-radius: var(--radius-lg);
  overflow: hidden; background: var(--surface-alt);
}
@media (max-width: 640px) { .map-embed { aspect-ratio: 4 / 3; } }
.map-embed iframe { width: 100%; height: 100%; border: 0; display: block; }
.map-placeholder {
  position: absolute; inset: 0;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: .7rem; padding: 1.5rem; text-align: center; color: var(--text-muted);
}
.map-placeholder p { margin: 0; font-size: .92rem; max-width: 40ch; }
.map-placeholder a { font-size: .85rem; }
```

- [ ] **Step 5: Substituir o iframe em `/contato`**

Trocar o bloco do `<iframe>` (linhas 115-130 de `app/(site)/contato/page.tsx`) por:

```tsx
<MapEmbed
  query={settings.address}
  titulo="Localização do escritório no Google Maps"
/>
```

Se a página ainda não lê `settings`, importar `getSettings` de `@/lib/reader`.

- [ ] **Step 6: Rodar e verificar que passa**

Run: `npm run e2e -- mapa`
Expected: PASS — 2 testes.

- [ ] **Step 7: Commit**

```bash
git add components/map-embed.tsx "app/(site)/contato/page.tsx" app/globals.css tests/e2e/mapa.spec.ts
git commit -m "fix: mapa carrega sob clique, com esqueleto de proporção fixa (P11)"
```

---

### Task 13: P12/P10 — logo dimensionado e imagens

**Files:**
- Modify: `components/site-header.tsx`
- Modify: `components/site-footer.tsx`
- Modify: `app/(site)/sobre/page.tsx`, `app/(site)/page.tsx` (props de `next/image`)
- Create: `components/backdrop.tsx`
- Modify: `app/globals.css`
- Create: `tests/e2e/imagens.spec.ts`

**Interfaces:**
- Consumes: nada
- Produces: `<Backdrop variant="grid" | "arcs" />` — grafismo SVG decorativo, sem requisição de rede

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/imagens.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('nenhuma imagem é servida com mais que o dobro do tamanho exibido', async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 900 });
  await page.goto('/', { waitUntil: 'networkidle' });

  const excessivas = await page.evaluate(() =>
    Array.from(document.images)
      .filter((img) => img.naturalWidth > 0)
      .map((img) => ({
        src: img.currentSrc,
        natural: img.naturalWidth,
        exibido: Math.round(img.getBoundingClientRect().width * window.devicePixelRatio),
      }))
      .filter((i) => i.exibido > 0 && i.natural > i.exibido * 2.2)
  );
  expect(excessivas, JSON.stringify(excessivas, null, 2)).toEqual([]);
});

test('toda imagem tem alt e dimensões explícitas', async ({ page }) => {
  await page.goto('/');
  const problemas = await page.evaluate(() =>
    Array.from(document.images)
      .filter((img) => !img.hasAttribute('alt') || !img.width || !img.height)
      .map((img) => img.currentSrc)
  );
  expect(problemas).toEqual([]);
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- imagens`
Expected: FAIL — o logo é servido a 1151px e exibido a 133px.

- [ ] **Step 3: Corrigir o dimensionamento do logo**

Em `components/site-header.tsx`, o `<Image>` do logo declara `width={1151} height={399}` — o que faz o Next servir a imagem grande. Trocar pelas dimensões reais de exibição (2× de 133×46):

```tsx
<Image
  className="brand-logo"
  src="/assets/img/logo-horizontal.png"
  alt="Douglas Senturião Advocacia"
  width={266}
  height={92}
  sizes="133px"
  priority
/>
```

Fazer o mesmo em `components/site-footer.tsx` para `.footer-logo` (altura 52px → `width={300} height={104}`, `sizes="150px"`).

**Limite registrado:** o SVG do §8.4 do spec depende do arquivo vetorial original, que não está no repositório. Esta tarefa corrige o superdimensionamento de 4×; o SVG fica como dependência do cliente.

- [ ] **Step 4: Revisar `sizes` nas fotos**

Acrescentar `sizes` a cada `next/image` que não tem:

- `app/(site)/page.tsx`, `.founder-photo` → `sizes="(min-width: 760px) 40vw, 92vw"`
- `app/(site)/sobre/page.tsx` → conferir cada `<Image>` e aplicar o `sizes` correspondente ao layout
- `components/hero-video.tsx` → já resolvido na Tarefa 6

- [ ] **Step 5: Criar `components/backdrop.tsx`**

Grafismos de apoio gerados em SVG inline — sem pessoas, sem ambientes, sem requisição de rede e sem risco de licença.

```tsx
/**
 * Grafismo decorativo de fundo. É SVG inline: não custa requisição, não
 * tem licença e não afirma nada sobre o escritório. Puramente ornamental.
 */
export function Backdrop({ variant = 'grid' }: { variant?: 'grid' | 'arcs' }) {
  if (variant === 'arcs') {
    return (
      <svg className="backdrop" viewBox="0 0 400 400" aria-hidden="true" focusable="false">
        {[80, 140, 200, 260, 320].map((r) => (
          <circle key={r} cx="400" cy="0" r={r} fill="none" stroke="currentColor" strokeWidth="1" />
        ))}
      </svg>
    );
  }
  return (
    <svg className="backdrop" viewBox="0 0 400 400" aria-hidden="true" focusable="false">
      <defs>
        <pattern id="dsa-grid" width="40" height="40" patternUnits="userSpaceOnUse">
          <path d="M40 0H0v40" fill="none" stroke="currentColor" strokeWidth="1" />
        </pattern>
      </defs>
      <rect width="400" height="400" fill="url(#dsa-grid)" />
    </svg>
  );
}
```

```css
.backdrop {
  position: absolute; inset: 0; width: 100%; height: 100%;
  color: var(--border-inverse); opacity: .35; pointer-events: none;
}
```

Montar em `.page-hero` e `.section--ink` como primeiro filho, com `position: relative` no pai (já existe).

- [ ] **Step 6: Rodar e verificar que passa**

Run: `npm run e2e -- imagens`
Expected: PASS — 2 testes.

Run: `npm run e2e -- orcamento`
Expected: PASS — o logo menor reduz o total da home.

- [ ] **Step 7: Commit**

```bash
git add components/site-header.tsx components/site-footer.tsx components/backdrop.tsx "app/(site)" app/globals.css tests/e2e/imagens.spec.ts
git commit -m "perf: logo no tamanho certo e grafismos SVG sem custo de rede (P12/P10)"
```

---

### Task 14: P13 — manifest, feed e páginas de erro

**Files:**
- Create: `app/manifest.ts`
- Create: `app/feed.xml/route.ts`
- Create: `app/not-found.tsx`
- Create: `app/error.tsx`
- Modify: `app/layout.tsx` (link do feed)
- Create: `tests/e2e/plataforma.spec.ts`

**Interfaces:**
- Consumes: `getPosts()` de `lib/reader.ts`
- Produces: `/manifest.webmanifest`, `/feed.xml`, páginas 404 e 500

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/plataforma.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('o manifest existe e é válido', async ({ request }) => {
  const res = await request.get('/manifest.webmanifest');
  expect(res.status()).toBe(200);
  const m = await res.json();
  expect(m.name).toContain('Senturião');
  expect(Array.isArray(m.icons)).toBe(true);
  expect(m.icons.some((i: { purpose?: string }) => i.purpose?.includes('maskable'))).toBe(true);
});

test('o feed do blog existe e lista artigos', async ({ request }) => {
  const res = await request.get('/feed.xml');
  expect(res.status()).toBe(200);
  expect(res.headers()['content-type']).toContain('xml');
  const xml = await res.text();
  expect(xml).toContain('<feed');
  expect((xml.match(/<entry>/g) ?? []).length).toBeGreaterThanOrEqual(10);
});

test('a 404 é desenhada e oferece caminhos', async ({ page }) => {
  const res = await page.goto('/rota-que-nao-existe');
  expect(res?.status()).toBe(404);
  await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
  await expect(page.getByRole('link', { name: /áreas de atuação/i })).toBeVisible();
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- plataforma`
Expected: FAIL — 404 em `/manifest.webmanifest` e `/feed.xml`.

- [ ] **Step 3: Criar `app/manifest.ts`**

```ts
import type { MetadataRoute } from 'next';

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'Douglas Senturião Advocacia',
    short_name: 'Senturião Adv.',
    description:
      'Estratégia jurídica para quem tem um conflito e um prazo. Direito Administrativo, cível e empresarial.',
    start_url: '/',
    display: 'standalone',
    lang: 'pt-BR',
    background_color: '#ffffff',
    theme_color: '#1d1b9a',
    icons: [
      { src: '/assets/img/favicon.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/assets/img/apple-touch-icon.png', sizes: '180x180', type: 'image/png', purpose: 'any' },
      { src: '/assets/img/logo-mark.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
    ],
  };
}
```

Conferir as dimensões reais dos PNGs com `file public/assets/img/*.png` e corrigir os `sizes` para os valores verdadeiros — um `sizes` mentiroso quebra a instalação no Android.

- [ ] **Step 4: Criar `app/feed.xml/route.ts`**

```ts
import { getPosts, getSettings } from '@/lib/reader';

const BASE = 'https://www.senturiaoadv.com.br';

function escapar(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

export async function GET() {
  const [posts, settings] = await Promise.all([getPosts(), getSettings()]);
  const atualizado =
    posts[0]?.publishedDate
      ? new Date(posts[0].publishedDate).toISOString()
      : new Date().toISOString();

  const entries = posts
    .map((p) => {
      const url = `${BASE}/blog/${p.slug}`;
      const data = p.publishedDate
        ? new Date(p.publishedDate).toISOString()
        : atualizado;
      return `  <entry>
    <title>${escapar(p.title)}</title>
    <link href="${url}"/>
    <id>${url}</id>
    <updated>${data}</updated>
    <summary>${escapar(p.description ?? '')}</summary>
    <category term="${escapar(p.area ?? '')}"/>
  </entry>`;
    })
    .join('\n');

  const xml = `<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>${escapar(settings.firmName)} — Conteúdo técnico</title>
  <link href="${BASE}/blog"/>
  <link rel="self" href="${BASE}/feed.xml"/>
  <id>${BASE}/</id>
  <updated>${atualizado}</updated>
  <author><name>${escapar(settings.lawyerName)}</name></author>
${entries}
</feed>`;

  return new Response(xml, {
    headers: {
      'Content-Type': 'application/atom+xml; charset=utf-8',
      'Cache-Control': 'public, s-maxage=3600, stale-while-revalidate=86400',
    },
  });
}
```

Declarar o feed em `app/layout.tsx`, dentro de `metadata`:

```ts
alternates: {
  types: { 'application/atom+xml': '/feed.xml' },
},
```

- [ ] **Step 5: Criar `app/not-found.tsx`**

Usa o design system existente; nenhuma copy nova além do necessário.

```tsx
import Link from 'next/link';

export default function NotFound() {
  return (
    <section className="section">
      <div className="container container--narrow">
        <p className="eyebrow">Erro 404</p>
        <h1>Esta página não existe.</h1>
        <p className="lead">
          O endereço pode ter mudado ou o link pode estar incompleto. Abaixo
          estão os caminhos mais usados.
        </p>
        <ul className="checklist">
          <li><Link href="/areas">Áreas de atuação</Link></li>
          <li><Link href="/blog">Conteúdo técnico</Link></li>
          <li><Link href="/diagnostico">Diagnóstico jurídico inicial</Link></li>
          <li><Link href="/contato">Contato</Link></li>
        </ul>
      </div>
    </section>
  );
}
```

- [ ] **Step 6: Criar `app/error.tsx`**

```tsx
'use client';

import Link from 'next/link';

export default function Error({ reset }: { error: Error; reset: () => void }) {
  return (
    <section className="section">
      <div className="container container--narrow">
        <p className="eyebrow">Erro inesperado</p>
        <h1>Algo falhou ao carregar esta página.</h1>
        <p className="lead">
          Tente novamente. Se o problema continuar e o seu caso tiver prazo em
          curso, fale direto com o escritório pelo{' '}
          <Link href="/contato">canal de contato</Link>.
        </p>
        <button type="button" className="btn btn-primary" onClick={reset}>
          Tentar novamente
        </button>
      </div>
    </section>
  );
}
```

- [ ] **Step 7: Rodar e verificar que passa**

Run: `npm run e2e -- plataforma`
Expected: PASS — 3 testes.

- [ ] **Step 8: Commit**

```bash
git add app/manifest.ts app/feed.xml app/not-found.tsx app/error.tsx app/layout.tsx tests/e2e/plataforma.spec.ts
git commit -m "feat: manifest, feed Atom e páginas de erro desenhadas (P13)"
```

---

### Task 15: Template padronizado de área (§8.1)

O principal ativo de SEO do site. Campos novos são **opcionais**: as 14 áreas continuam renderizando sem regressão.

**Files:**
- Modify: `keystatic.config.ts`
- Modify: `app/(site)/areas/[slug]/page.tsx`
- Modify: `lib/reader.ts` (artigos relacionados)
- Modify: `app/globals.css`
- Modify: `content/areas/mandado-de-seguranca.mdoc` (uma área preenchida como referência)
- Create: `tests/e2e/area.spec.ts`

**Interfaces:**
- Consumes: `getPosts()` de `lib/reader.ts`
- Produces: `getPostsByArea(areaKey: string): Promise<PostEntry[]>` em `lib/reader.ts`; campos Keystatic `deadlines: { label, prazo, base }[]` e `decisive: { title, body }[]`

- [ ] **Step 1: Escrever o teste E2E**

`tests/e2e/area.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('as 14 áreas renderizam sem erro mesmo sem os campos novos', async ({ page, request }) => {
  const sitemap = await (await request.get('/sitemap.xml')).text();
  const slugs = [...sitemap.matchAll(/\/areas\/([a-z0-9-]+)</g)].map((m) => m[1]);
  expect(slugs.length).toBe(14);

  for (const slug of slugs) {
    const res = await page.goto(`/areas/${slug}`);
    expect(res?.status(), `/areas/${slug}`).toBe(200);
    await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
  }
});

test('a área de referência mostra o bloco de prazo e o que decide o caso', async ({ page }) => {
  await page.goto('/areas/mandado-de-seguranca');
  await expect(page.getByRole('heading', { name: /o prazo/i })).toBeVisible();
  await expect(page.locator('.deadline-item')).not.toHaveCount(0);
  await expect(page.getByRole('heading', { name: /costuma decidir/i })).toBeVisible();
});

test('a área lista artigos relacionados da mesma taxonomia', async ({ page }) => {
  await page.goto('/areas/mandado-de-seguranca');
  const relacionados = page.locator('.related-posts a');
  await expect(relacionados).not.toHaveCount(0);
  const hrefs = await relacionados.evaluateAll((els) =>
    els.map((e) => e.getAttribute('href') ?? '')
  );
  expect(hrefs.every((h) => h.startsWith('/blog/'))).toBe(true);
});

test('o schema FAQPage está presente onde há FAQ', async ({ page }) => {
  await page.goto('/areas/mandado-de-seguranca');
  const tipos = await page.evaluate(() =>
    Array.from(document.querySelectorAll('script[type="application/ld+json"]'))
      .map((s) => JSON.parse(s.textContent ?? '{}')['@type'])
  );
  expect(tipos).toContain('FAQPage');
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `npm run e2e -- area`
Expected: FAIL nos testes 2 e 3 (blocos não existem). O teste 1 e o 4 já devem passar — `FAQPage` já está implementado em `app/(site)/areas/[slug]/page.tsx:44-52`. Se o teste 1 falhar, há regressão de conteúdo e é preciso parar e investigar antes de seguir.

- [ ] **Step 3: Estender o schema Keystatic**

Em `keystatic.config.ts`, dentro do `schema` da coleção `areas`, depois de `notice`:

```ts
        deadlinesTitle: fields.text({
          label: 'Título da seção de prazos',
          defaultValue: 'O prazo',
        }),
        deadlines: fields.array(
          fields.object({
            label: fields.text({ label: 'Situação' }),
            prazo: fields.text({ label: 'Prazo' }),
            base: fields.text({ label: 'Base legal', multiline: true }),
          }),
          {
            label: 'Prazos legais da matéria',
            description:
              'O ativo mais distintivo do site. Cada item vira uma linha do bloco de prazos.',
            itemLabel: (props) => `${props.fields.label.value} — ${props.fields.prazo.value}`,
          }
        ),
        decisiveTitle: fields.text({
          label: 'Título da seção técnica',
          defaultValue: 'O que costuma decidir o caso',
        }),
        decisive: fields.array(
          fields.object({
            title: fields.text({ label: 'Ponto' }),
            body: fields.text({ label: 'Explicação', multiline: true }),
          }),
          {
            label: 'O que costuma decidir o caso',
            itemLabel: (props) => props.fields.title.value,
          }
        ),
        areaKey: fields.text({
          label: 'Chave da área (para artigos relacionados)',
          description:
            'Deve bater com o campo "Chave da área" usado nos artigos do blog.',
          defaultValue: '',
        }),
```

- [ ] **Step 4: Adicionar `getPostsByArea` em `lib/reader.ts`**

```ts
/** Artigos da mesma taxonomia de uma área, do mais recente para o mais antigo. */
export async function getPostsByArea(areaKey: string, limite = 3) {
  if (!areaKey) return [];
  const posts = await getPosts();
  return posts.filter((p) => p.areaKey === areaKey).slice(0, limite);
}
```

- [ ] **Step 5: Reestruturar `app/(site)/areas/[slug]/page.tsx`**

Manter tudo o que já existe e inserir, entre a seção `.split` atual e a de FAQ, os blocos novos — todos condicionais:

```tsx
{area.deadlines.length > 0 && (
  <section className="section section--alt">
    <div className="container">
      <Reveal className="section-head">
        <p className="eyebrow">Prazos</p>
        <h2>{area.deadlinesTitle || 'O prazo'}</h2>
        <p className="lead">
          Mover-se na semana errada pode custar o caso. Estes são os prazos que
          orientam a reação nesta matéria.
        </p>
      </Reveal>
      <ul className="deadline-list">
        {area.deadlines.map((d, i) => (
          <li className="deadline-item" key={i}>
            <p className="deadline-prazo" data-prazo>{d.prazo}</p>
            <div>
              <h3>{d.label}</h3>
              {d.base && <p className="hint">{d.base}</p>}
            </div>
          </li>
        ))}
      </ul>
      <p className="hint" style={{ marginTop: '1.2rem' }}>
        Informação de caráter geral. Prazos variam conforme o caso concreto e a
        contagem depende da data de ciência — não substitui análise individualizada.
      </p>
    </div>
  </section>
)}

{area.decisive.length > 0 && (
  <section className="section">
    <div className="container">
      <Reveal className="section-head">
        <p className="eyebrow">Técnica</p>
        <h2>{area.decisiveTitle || 'O que costuma decidir o caso'}</h2>
      </Reveal>
      <div className="deflist">
        {area.decisive.map((d, i) => (
          <div className="item" key={i}>
            <h3>{d.title}</h3>
            <p>{d.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
)}

{relacionados.length > 0 && (
  <section className="section section--alt">
    <div className="container">
      <Reveal className="section-head">
        <p className="eyebrow">Conteúdo técnico</p>
        <h2>Artigos sobre {area.title.toLowerCase()}.</h2>
      </Reveal>
      <div className="post-list related-posts">
        {relacionados.map((p, i) => (
          <RevealItem key={p.slug} index={i}>
            <Link className="card post-card" href={`/blog/${p.slug}`}>
              <span className="post-tag">{p.area}</span>
              <h3>{p.title}</h3>
              <p>{p.description}</p>
              <span className="post-meta"><span>{p.readingTime}</span></span>
            </Link>
          </RevealItem>
        ))}
      </div>
    </div>
  </section>
)}
```

No topo do componente, buscar os relacionados:

```tsx
const relacionados = await getPostsByArea(area.areaKey ?? '');
```

Acrescentar o aviso legal do §8.1 item 9 ao final, antes do `cta-band` — reaproveitando a `.notice` já existente no design system, com o texto de conformidade que já está no rodapé. **Não escrever aviso novo**: copiar o texto vigente do rodapé para manter a conformidade já aprovada.

- [ ] **Step 6: Estilizar o bloco de prazos**

```css
.deadline-list { list-style: none; padding: 0; margin: 0; display: grid; gap: .8rem; }
.deadline-item {
  display: grid; gap: .3rem 1.2rem; align-items: baseline; margin: 0;
  background: var(--surface-raised); border: 1px solid var(--border);
  border-left: 3px solid var(--urgent); border-radius: var(--radius-lg);
  padding: 1.1rem 1.3rem;
}
@media (min-width: 700px) { .deadline-item { grid-template-columns: 10rem 1fr; } }
.deadline-prazo {
  margin: 0; font-family: var(--font-display-stack); font-weight: 700;
  font-size: 1.15rem; color: var(--urgent); font-variant-numeric: tabular-nums;
}
.deadline-item h3 { font-size: 1.02rem; margin: 0 0 .2rem; }
.deadline-item .hint { margin: 0; }
```

A cor `--urgent` aparece só aqui — é o uso parcimonioso que o §3.1 do briefing exige.

- [ ] **Step 7: Preencher a área de referência**

Em `content/areas/mandado-de-seguranca.mdoc`, acrescentar ao frontmatter (sem alterar nenhum campo existente):

```yaml
areaKey: mandado-de-seguranca
deadlinesTitle: O prazo
deadlines:
  - label: Impetração do mandado de segurança
    prazo: 120 dias
    base: >-
      Prazo decadencial contado da ciência do ato impugnado — art. 23 da Lei
      12.016/2009. Não se suspende nem se interrompe.
  - label: Agravo de instrumento contra decisão sobre liminar
    prazo: 15 dias
    base: >-
      Art. 1.003, §5º, e art. 1.015, I, do CPC, contados da intimação da
      decisão.
  - label: Informações da autoridade coatora
    prazo: 10 dias
    base: Art. 7º, I, da Lei 12.016/2009, contados da notificação.
decisiveTitle: O que costuma decidir o caso
decisive:
  - title: Prova pré-constituída
    body: >-
      O mandado de segurança não admite dilação probatória. O direito precisa
      estar demonstrado por documento já existente no momento da impetração —
      é isso que define o cabimento antes de qualquer discussão de mérito.
  - title: Identificação correta da autoridade coatora
    body: >-
      Apontar a autoridade errada leva à extinção sem resolução de mérito. A
      coatora é quem pratica o ato ou detém competência para corrigi-lo, não
      necessariamente o superior hierárquico.
  - title: Delimitação do ato impugnado
    body: >-
      Atacar o ato certo, na data certa, é o que mantém o prazo aberto. Ato
      complexo, ato de execução e ato normativo têm marcos temporais distintos.
```

Preencher `areaKey` nas outras 13 áreas com o valor correspondente ao `areaKey` dos artigos, deixando `deadlines` e `decisive` vazios — o Douglas preenche pelo CMS. **Não inventar prazos.** Apenas esta área, cujos prazos já constam do conteúdo existente do site e da lei citada, sai preenchida como referência.

- [ ] **Step 8: Rodar e verificar que passa**

Run: `npm run e2e -- area`
Expected: PASS — 4 testes.

Run: `npm run e2e`
Expected: PASS.

Verificar em `/keystatic` que os campos novos aparecem e são editáveis.

- [ ] **Step 9: Commit**

```bash
git add keystatic.config.ts "app/(site)/areas/[slug]/page.tsx" lib/reader.ts app/globals.css content/areas tests/e2e/area.spec.ts
git commit -m "feat: template padronizado de área com bloco de prazos (8.1)"
```

---

### Task 16: Acessibilidade no CI e verificação final

Fecha os critérios de aceite do §11 que ainda não têm teste.

**Files:**
- Create: `tests/e2e/acessibilidade.spec.ts`
- Create: `.github/workflows/ci.yml`
- Create: `docs/superpowers/plans/2026-08-31-sprints-1-3-resultados.md`

**Interfaces:**
- Consumes: todas as tarefas anteriores
- Produces: pipeline de CI que falha em violação séria ou crítica

- [ ] **Step 1: Escrever os testes de acessibilidade**

`tests/e2e/acessibilidade.spec.ts`:

```ts
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

const ROTAS = [
  '/', '/sobre', '/areas', '/areas/mandado-de-seguranca',
  '/blog', '/modelos', '/diagnostico', '/contato',
  '/politica-de-privacidade', '/aviso-publicidade',
];

for (const rota of ROTAS) {
  test(`axe-core sem violação séria ou crítica em ${rota}`, async ({ page }) => {
    await page.goto(rota);
    // Fecha o banner de consentimento para auditar a página, não o overlay.
    const recusar = page.getByRole('button', { name: 'Recusar todos' });
    if (await recusar.isVisible().catch(() => false)) await recusar.click();

    const r = await new AxeBuilder({ page })
      .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
      .analyze();

    const graves = r.violations.filter((v) =>
      v.impact === 'serious' || v.impact === 'critical'
    );
    expect(
      graves,
      graves.map((v) => `${v.id}: ${v.nodes.length} nó(s) — ${v.help}`).join('\n')
    ).toEqual([]);
  });
}

test('o site é operável só pelo teclado, da home ao envio do diagnóstico', async ({ page }) => {
  await page.goto('/');
  await page.keyboard.press('Tab');
  await expect(page.getByRole('link', { name: /ir para o conteúdo/i })).toBeFocused();

  // Chega ao CTA principal só com Tab.
  let achou = false;
  for (let i = 0; i < 40 && !achou; i++) {
    await page.keyboard.press('Tab');
    achou = await page
      .getByRole('link', { name: /solicitar diagnóstico inicial/i })
      .evaluate((el) => el === document.activeElement)
      .catch(() => false);
  }
  expect(achou, 'o CTA principal não foi alcançado por teclado').toBe(true);
  await page.keyboard.press('Enter');
  await expect(page).toHaveURL(/\/diagnostico/);
});

test('o menu mobile fecha com Esc e devolve o foco ao botão', async ({ page }) => {
  await page.setViewportSize({ width: 390, height: 844 });
  await page.goto('/');
  const botao = page.getByRole('button', { name: /menu de navegação/i });
  await botao.click();
  await expect(botao).toHaveAttribute('aria-expanded', 'true');
  await page.keyboard.press('Escape');
  await expect(botao).toHaveAttribute('aria-expanded', 'false');
  await expect(botao).toBeFocused();
});

test('os avisos da OAB estão em todas as páginas', async ({ page }) => {
  for (const rota of ROTAS) {
    await page.goto(rota);
    await expect(
      page.getByText(/Provimento/i).first(),
      `${rota} sem aviso de publicidade`
    ).toBeVisible();
  }
});
```

- [ ] **Step 2: Rodar e corrigir o que aparecer**

Run: `npm run e2e -- acessibilidade`
Expected: FAIL na primeira execução. Cada violação séria ou crítica é corrigida no componente que a origina — não silenciada por regra de exceção do axe.

O teste do menu mobile provavelmente falha: `components/site-header.tsx` não trata `Escape` nem devolve o foco. Implementar:

```tsx
useEffect(() => {
  if (!open) return;
  const onKey = (e: KeyboardEvent) => {
    if (e.key === 'Escape') {
      setOpen(false);
      toggleRef.current?.focus();
    }
  };
  document.addEventListener('keydown', onKey);
  return () => document.removeEventListener('keydown', onKey);
}, [open]);
```

com `const toggleRef = useRef<HTMLButtonElement>(null)` aplicado ao `<button className="nav-toggle">`.

Outro ponto provável: o `*` de campo obrigatório em `components/diagnostico-form.tsx` é `aria-hidden` e a obrigatoriedade fica só na cor. Acrescentar texto acessível:

```tsx
<span className="req" aria-hidden="true">*</span>
<span className="sr-only">(obrigatório)</span>
```

```css
.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
  overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0;
}
```

- [ ] **Step 3: Criar o pipeline de CI**

`.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Testes unitários
        run: npm run test
      - name: Build
        run: npm run build
      - name: Testes E2E e acessibilidade
        run: npm run e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
```

- [ ] **Step 4: Verificação manual e registro dos números**

Estes critérios do §11 não são automatizáveis. Medir e **registrar o resultado real**, mesmo que abaixo da meta:

```bash
npm run build && npm run start
# Em outro terminal, com o Chrome instalado:
npx lighthouse http://localhost:3100 \
  --preset=desktop --quiet --chrome-flags="--headless" \
  --output=json --output-path=/tmp/dsa/lh-desktop.json
npx lighthouse http://localhost:3100 \
  --form-factor=mobile --throttling-method=simulate --quiet \
  --chrome-flags="--headless" --output=json --output-path=/tmp/dsa/lh-mobile.json
```

Registrar em `docs/superpowers/plans/2026-08-31-sprints-1-3-resultados.md`:

- Lighthouse mobile: Performance, Acessibilidade, Boas Práticas, SEO (metas ≥95)
- LCP, INP, CLS em 4G throttled (metas ≤1,8 s, ≤150 ms, ≤0,05)
- Peso total da home e maior asset (metas ≤900 KB, ≤600 KB)
- Navegadores testados: Chrome, Safari, Firefox, Edge, iOS Safari, Chrome Android
- Larguras testadas: 360, 390, 768, 1024, 1440, 1920 px
- Itens que ficaram abaixo da meta, com a razão

**Se uma meta não for atingida, o registro diz isso explicitamente.** Um número inventado é pior que uma meta não batida.

- [ ] **Step 5: Verificar a não-regressão de conteúdo**

```bash
git diff --stat 56fd87d..HEAD -- content/
git diff 56fd87d..HEAD -- content/
```

Expected: as únicas mudanças em `content/` são os campos novos e opcionais da Tarefa 15 (`areaKey`, `deadlines*`, `decisive*`). Nenhuma linha de copy existente alterada ou removida.

```bash
git diff 56fd87d..HEAD -- components/site-footer.tsx | grep -E '^[-+].*(Provimento|8\.906|205/2021|OAB)'
```

Expected: nenhuma linha removida (`-`) com esses termos.

- [ ] **Step 6: Rodar a suíte inteira**

Run: `npm run test && npm run build && npm run e2e`
Expected: PASS em tudo.

- [ ] **Step 7: Commit**

```bash
git add tests/e2e/acessibilidade.spec.ts .github/workflows/ci.yml components/site-header.tsx components/diagnostico-form.tsx app/globals.css docs/superpowers/plans/2026-08-31-sprints-1-3-resultados.md
git commit -m "test: acessibilidade no CI e registro dos números de verificação"
```

---

## Cobertura do spec

| Seção do spec | Tarefa |
|---------------|--------|
| §2.1 duas camadas de token | 2 |
| §2.2 três estados de tema | 2, 3 |
| §2.3 contraste AAA no corpo | 2 (teste unitário) |
| §2.4 tipografia e fontes self-hosted | 5 |
| §3 P1 reveal | 4 |
| §4 P2 hero | 6 |
| §5 P7 segurança e CSP | 9 (depende de 3 e 8) |
| §5.4 cache de CDN | 9 |
| §6 P6 medição e consentimento | 7, 8 |
| §7 template de área | 15 |
| §8.1 P8 cards | 10 |
| §8.2 P9 colunas vazias | 11 |
| §8.3 P11 mapa | 12 |
| §8.4 P12 logo | 13 |
| §8.5 P10 imagens e grafismos | 13 |
| §8.6 P13 manifest, feed, erros, dark mode | 14 (dark mode em 2/3) |
| §9 verificação | 1, 16 |
| §10 não-regressão | 16 (Step 5) |
| §11 dependências do cliente | registradas no spec; nenhuma tarefa as presume resolvidas |
